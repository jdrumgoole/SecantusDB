# Security Review — 2026-09-26

Full walk of `scripts/security-review-prompt.md` (sections 1–13) against HEAD
`431d747c`. Diff-based against the previous cycle's reviewed HEAD (`1b935c1e`):

```
git diff --stat 1b935c1e..431d747c -- . ':!docs/security-reports'
```

returns **empty**. `git log --oneline 1b935c1e..431d747c` shows exactly one
commit: `431d747c`, which is the previous cycle's own report PR merge
(`Security Review — 2026-09-25 (#1568)`) — a docs-only addition of
`docs/security-reports/2026-09-25.md`. Confirmed by targeted diffs that
**zero** production surface moved:

```
git diff --stat 1b935c1e..431d747c -- .github/workflows/                                                  # empty
git diff --stat 1b935c1e..431d747c -- pyproject.toml uv.lock 'crates/*/Cargo.toml' 'crates/*/Cargo.lock'  # empty
git diff --stat 1b935c1e..431d747c -- src/secantus/wire.py src/secantus/server.py \
  src/secantus/storage.py src/secantus/auth.py src/secantus/commands.py src/secantus/query.py \
  crates/secantus-wire crates/secantus-storage crates/secantus-wt \
  crates/secantus-pgserver crates/secantus-pgplan crates/secantus-core                                    # empty
```

This is therefore a true zero-diff cycle — nothing in the tree that this
review's scope treats as production surface changed since yesterday's report
merged. Rather than restate the previous report's findings from memory, the
three standing CRITICALs were **re-read at their cited line numbers against
current HEAD** this cycle (not inferred from the empty diff — a diff proves
no *edit* happened, not that a *fix* landed under a different name, so each
site was opened fresh):

- `crates/secantus-wire/src/lib.rs:100-151` (`Header::body_len`) — read in
  full. Still exactly as filed.
- `crates/secantus-server/src/lib.rs:70-109` (`AllocBudget::acquire`) and
  `:553-562` (the call site) — read in full this cycle for the first time in
  several reports (earlier ones cited it from the previous read); confirms
  the `while *used != 0 && *used + n > self.cap` skip-on-empty condition and
  the unguarded `vec![0u8; body_len]` immediately after.
- `src/secantus/commands.py:8834-8917` (`_COMMAND_ACTIONS` dict, read in
  full this cycle) and `:9747-9765` (the RBAC gate) — `bulkWrite` confirmed
  absent from every key in the 84-entry table; the two other hits for the
  literal string `"bulkWrite"` in that neighbourhood (`:9297`, `:9398`) are
  the unrelated `_API_V1_COMMANDS` and transaction-blocklist sets, not the
  RBAC table.
- `src/secantus/query.py:1400-1471` (`_compile_regex` / `_op_regex`) — read
  in full. The 1000-char cap and the TypeError-recovery fallback's
  second, uncapped `re.compile` are both still present verbatim.

A fresh dependency/CVE sweep (WebSearch against pymongo, `cryptography`,
`rustls`/RUSTSEC, and starlette/FastAPI, since advisories are time-sensitive
independent of code diff) found **nothing new**: the same four tracked items
from the last several cycles (pymongo CVE-2026-96748 and CVE-2026-88029,
`cryptography` CVE-2026-69247/69248/69249, RUSTSEC-2026-0285) are the only
hits, all already filed at their existing severity, all pins unchanged. No
new CRITICAL or WARNING finding this cycle; no new issues filed.

---

## 🔴 CRITICAL (Immediate action required)

All three re-confirmed by fresh direct reads against current HEAD (see above):

- **#995** — `crates/secantus-wire/src/lib.rs:100-151` (`Header::body_len`).
  `message_length` is `pub message_length: i32`; `body_len()` does
  `if (self.message_length as usize) < HEADER_SIZE { ... }` first — for a
  negative `message_length` (e.g. `-1`) this sign-extends to
  `usize::MAX - n`, which is `>= HEADER_SIZE`, so the floor guard does not
  fire; `if self.message_length > MAX_MESSAGE_SIZE` next compares the
  still-negative signed `i32` against a positive bound, which is never true,
  so the ceiling guard does not fire either. The final line
  `Ok(self.message_length as usize - HEADER_SIZE)` returns the same huge
  value minus 16. The caller (`crates/secantus-server/src/lib.rs:553-562`)
  feeds that straight to `shared.alloc_budget.acquire(body_len)` — whose
  `while *used != 0 && *used + n > self.cap` loop **skips its own wait
  whenever `*used == 0`**, i.e. on an otherwise-idle server — and then to
  `vec![0u8; body_len]`. Rust's default global allocator aborts the process
  on an allocation failure at this size. One unauthenticated 16-byte frame
  (a crafted header with a negative `message_length`) takes the whole
  Mongo-wire Rust server down. **Not fixed.**
- **#1160** — `src/secantus/commands.py:8834-8917` (`_COMMAND_ACTIONS`
  table), `:8924` (`_NO_PRIVILEGE_COMMANDS`), `:9747-9765` (the RBAC gate).
  The gate is `info = _COMMAND_ACTIONS.get(name); if info is not None: ...`
  — skip-on-unknown rather than deny-on-unknown — and `bulkWrite` is not a
  key anywhere in the 84-entry `_COMMAND_ACTIONS` literal (confirmed by a
  full read of the dict this cycle, not just a grep). An authenticated
  connection with zero role grants under `--auth` can still call
  `bulkWrite` and have every one of its constituent insert/update/delete
  ops executed with no privilege check at all. **Not fixed.**
- **#977** — `src/secantus/query.py:1400-1471`. `_compile_regex`'s only
  gate is `len(pattern) > _MAX_REGEX_PATTERN_LEN` (1000 chars) ahead of an
  unbounded `re.compile(pattern, flags)` — no backtracking-depth limit, no
  compile/match timeout — and the `TypeError`-recovery fallback inside
  `_op_regex` (triggered by an unhashable pattern argument) does a second,
  uncapped `re.compile(regex_pattern, flags)` that bypasses even the length
  check. A short pathological pattern (e.g. `(a+)+b`) against an
  adversarial non-matching string pins a worker thread's CPU indefinitely
  via an unauthenticated `find`. **Not fixed.**

---

## 🟠 WARNING (Address within 1 week)

**No new finding this cycle.** All 50 standing WARNING-tier issues are
carried forward unchanged; every file underneath them (`src/secantus/`, all
`crates/`) is zero-diff since the last cycle, so there is nothing to re-scan
line by line this time beyond the RBAC table already re-read above under
CRITICAL/§6:

- **RBAC / dispatch-fallthrough** (6): `killSessions`/`killAllSessions`/
  `killAllSessionsByPattern` (**#1226**) · SQL `CALL`/`CREATE PROCEDURE`/`DROP
  PROCEDURE` (**#996**) · SQL `PREPARE`/`EXECUTE` (**#948**) · multi-name
  `DROP TABLE` (**#947**) · plpgsql trigger bodies (**#909**) · `ROLLBACK TO
  SAVEPOINT` index-DDL restore (**#1118**).
- **Rust unbounded-recursion class** (19): #1355, #1356, #1357, #1394, #1395,
  #1396, #1430, #1431, #1432, #1433, #1117, #1161, #1192, #1193, #1550,
  #1564, #1456, #1454 — plus **#1455** (a reachable-panic sibling of the
  same `parse_date_text` family, tracked alongside the recursion entries).
- **`declared_typmod` unchecked arithmetic, debug-build panic** (1): **#1549**.
- **Schema-qualified function catalog dict-key collision** (1): **#1551**.
- **PG-server standing gaps** (9): #1191, #1194, #1195, #1196, #949, #976,
  #1451, #1452, #1453.
- **Other standing items** (8): #1227, #1228, #1229, #1230, #1231, #1037,
  #1073, #1024 / #1023 (dependency posture, also cross-referenced under INFO
  below since they originate from the CVE sweep but remain filed as issues).
- **Reachable panic, PostGIS WKT parser** (1): #1471.
- **Exact-numeric-at-any-width cost gaps** (2): #1521, #1522.
- **CI permissions gaps** (1 more): #910 (`validate.yml`'s `pgjdbc-report`
  job), #978 (`rust-wheels.yml`).

`gh issue list --label security --state open` returns **53** issues; 3 are
the CRITICALs above (individually re-confirmed `OPEN` this cycle via
`gh issue view`), the remaining **50** are this WARNING inventory —
`53 - 3 = 50` exactly, unchanged from the last several cycles.

---

## 🟡 INFO (Best-practice recommendations)

Fresh dependency/CVE sweep this cycle (all manifests zero-diff — confirmed by
targeted `git diff --stat` against `pyproject.toml`, `uv.lock`, and every
`Cargo.toml`/`Cargo.lock` — so this is purely "did a new advisory get
published against an unchanged pin"):

- `pymongo` CVE-2026-96748 (connection-string host-list injection, fixed at
  4.18.2; this repo pins `4.17.0`) — re-confirmed still open, still
  **unreachable**: the sole `MongoClient(` construction in
  `src/secantus/admin/client.py:171` takes the admin operator's own
  complete, self-typed URI (traced through `admin/routers/server.py` /
  `admin/history.py`), never a third-party-supplied fragment interpolated
  into a URI the app builds — which is the CVE's actual exploit shape. Still
  INFO, not WARNING.
- `pymongo` CVE-2026-88029 (GridFS NoSQL injection; dependency used only for
  its bundled `bson` codec, never as a GridFS client over attacker input;
  pin `4.17.0` unchanged, fixed at 4.18.1) — re-confirmed still open, still
  unreachable, still INFO.
- `anyio` CVE-2026-63349, CVE-2026-64847, and `cryptography` CVE-2026-69247
  (unreachable: no `anyio.to_process`/`run_process`/`open_process`/
  process-pool usage and no `pkcs7_*` decrypt call anywhere in
  `src/secantus/`, re-confirmed by fresh grep this cycle) — re-confirmed
  still open, still unreachable, still INFO. WebSearch found no new advisory
  against `anyio` or `cryptography` published this cycle.
- Starlette CVE-2026-48710 ("BadHost") — re-confirmed already remediated:
  `pyproject.toml:92-99`'s explicit `starlette>=1.0.1` floor names this CVE
  and resolves to `1.3.1` in `uv.lock`, unchanged. No action needed.
- RUSTSEC — WebSearch for anything new against `tokio`/`rustls`/the
  WT-adjacent crates surfaced nothing beyond the already-tracked and
  already-fixed `RUSTSEC-2026-0285` (`rustls` 0.23.45, this repo's pin,
  already past the fix). `cargo-audit.yml` is a weekly Monday cron; last run
  2026-09-21 completed `success`, no `Cargo.lock` has moved since, next
  scheduled firing 2026-09-28.
- `cryptography` CVE-2026-69248/69249 (**#1023**, open, fixed at 49.0.0) and
  the wider CVE-2026-69247 (fixed only at 50.0.0) — unchanged remediation
  target, pin still `48.0.0`.
- `.github/workflows/publish.yml` missing `permissions:` on non-terminal
  jobs (**#1229**), `rust-wheels.yml` (**#978**), `validate.yml`'s
  `pgjdbc-report` job (**#910**) — all three files zero-diff this cycle.
- `.gitignore` — re-confirmed unchanged: covers `.env`/`*.pem`/`*.key`/
  `*.p12`/`*.pfx`/`.coverage`/`htmlcov/`/`.venv`/per-crate `target/` dirs.
- macOS wheel build's `tools/build_lz4_macos.sh` (**#1024**) still `curl`s a
  GitHub tag tarball with no checksum verification. Zero diff this cycle.

---

## 🟢 CLEAN (Passed review)

| Area | Finding |
|---|---|
| §1 Dependency extras hygiene | `pyproject.toml`/`uv.lock` zero-diff since last cycle. `admin` extra's web stack (`fastapi`, `starlette`, `uvicorn`, `jinja2`, `httpx`, `python-multipart`) still confined to `[project.optional-dependencies]`, confirmed still absent from the base install. CVE sweep found nothing new. |
| §2 Hardcoded secrets scan | `git diff 1b935c1e..431d747c -- . ':!docs/security-reports'` is empty, so the secrets patterns (`BEGIN (RSA\|EC\|PRIVATE\|CERTIFICATE)`, `api_key/token/password/secret = "`, credentialed `mongodb://` URIs, `AKIA`, `sk-` tokens) have nothing new to scan. `git log 1b935c1e..431d747c --diff-filter=A -- '*.env' '*.key' '*.pem' '*.p12' '*.pfx' '*secret*' '*credential*'` → empty (only the report markdown file was added). `.gitignore` unregressed. |
| §3 wire-protocol parsing (`wire.py`, `crates/secantus-wire`) | Zero-diff. `crates/secantus-wire/src/lib.rs` and the `crates/secantus-server/src/lib.rs` call site re-read in full for #995 above. |
| §4 storage-engine & FFI (`storage.py`, `crates/secantus-wt`, `crates/secantus-storage`) | Zero-diff. WT submodule gitlink unchanged (`eaa187c8`, confirmed via `git submodule status vendor/wiredtiger`, matching the pin recorded in every prior report). |
| §5 authentication (`auth.py`, SCRAM/X509 handshake in `commands.py`) | Zero-diff. |
| §6 command dispatch & operator engines | `commands.py` zero-diff — `_COMMAND_ACTIONS`/`_NO_PRIVILEGE_COMMANDS` fully re-read for #1160 confirms no row added or removed since last cycle. `query.py`/`update.py`/`expressions.py`(Python)/`aggregate.py`/`projection.py` all zero-diff. `crates/secantus-core`, `crates/secantus-pgplan`, `crates/secantus-pgserver` all zero-diff since last cycle. |
| §7 resource exhaustion (`cursors.py`, oplog/change-stream retention) | Zero-diff. |
| §8 network exposure & defaults (`server.py` bind defaults, TLS) | Zero-diff. |
| §9 Rust server `unsafe`/`panic!`/`unwrap()` sweep | No changed Rust file this cycle; nothing new to sweep. |
| §10 admin UI / FastAPI surface | Zero-diff on the app code. The single `MongoClient(...)` construction (`admin/client.py:171`) re-traced this cycle while re-checking the pymongo CVE above — still only ever receives the operator's own locally-supplied URI. |
| §11 Git history | `git log --oneline 1b935c1e..431d747c` — exactly one commit, the prior cycle's own report-PR merge. No commit touches `wire.py`/`server.py`/`storage.py`/`auth.py`/the Rust wire/dispatch/commands crates/workflows. |
| §12 GitHub Actions workflows | 14 workflow files present, unchanged count and unchanged content (`git diff --stat 1b935c1e..431d747c -- .github/workflows/` is empty). `security-review.yml` itself unchanged: still exactly `contents: write`, `pull-requests: write`, `issues: write`, `actions: read`. `cargo-audit.yml` ran clean on its last scheduled firing (2026-09-21) with no `Cargo.lock` movement since to warrant an off-cycle run. |
| §13 PyPI publish/version state | Live query (`pypi.org/pypi/SecantusDB/json`): latest version `0.6.0b16`, matches HEAD's `src/secantus/__init__.py` (unchanged); zero yanked releases. Rust Mongo-server crates unchanged at `0.5.3-beta.165` (tag `secantusdb-v0.5.3-beta.165` present); PG-server crates unchanged at `0.1.0-beta.2` (tag `secantusd-pg-v0.1.0-beta.2` present) — both confirmed against current `Cargo.toml` values. No wheel-content change (build config zero-diff). |
| Open-issue / branch reconciliation | `gh issue list --label security --state open` returns **53** at the start of this cycle, matching the 2026-09-25 report's closing count exactly (3 CRITICAL + 50 WARNING = 53, individually confirmed open via `gh issue view 995/1160/977`). `gh pr list --state open` shows no `security-review/*` PR (the one hit, #1482, is an unrelated dependabot GitHub-Actions bump). `git ls-remote --heads origin` shows no `security-review/*` branch. Clean starting state. |

---

## 📊 Summary

- **Date of review:** 2026-09-26.
- **SecantusDB version reviewed:** Python server `0.6.0b16`
  (`src/secantus/__init__.py`, unchanged this cycle); Rust Mongo-server
  crates lockstep at `0.5.3-beta.165` (unchanged); Rust PostgreSQL server
  crates (`secantus-pgserver`/`secantus-pgplan`/`secantus-pgcatalog`)
  independently versioned `0.1.0-beta.2` (unchanged).
- **Total issues by severity:** 3 CRITICAL (all carried forward, re-verified
  present by fresh direct reads against current HEAD — `#995`, `#1160`,
  `#977`) · **50** WARNING (all carried forward, none new) · 8 INFO items
  tracked in narrative (all previously known, none new this cycle) · large
  CLEAN inventory. **Total open `security`-labelled issues after this
  cycle: 53** (unchanged — no new filing, since no new CRITICAL/WARNING
  surfaced).
- **Comparison with the previous report (2026-09-25):** That cycle covered
  three commits, two of them real (a benchmark-numbers refresh and a
  changelog collation, both docs/website only). This cycle covers exactly
  one commit — the previous report's own PR merge — a true zero-diff cycle
  with no non-report file touched at all. No CRITICAL finding changed state;
  no WARNING finding changed state; the CVE/RUSTSEC sweep found nothing
  newly published against any tracked dependency.
- **Top 3 priorities for the team:**
  1. **The two whole-server-DoS-from-one-request bugs remain unmoved for at
     least twenty review cycles** — `#995`'s Rust `body_len` cast-before-
     check and `#977`'s regex GIL-starvation. Both remain a single crafted
     request away from taking the whole Mongo-wire server offline,
     unauthenticated in `#995`'s case.
  2. **`#1191` — the Rust PostgreSQL server has no authentication or
     authorization at all — remains completely untouched.** Every one of
     the 19 tracked unbounded-recursion sites in that server is reachable
     pre-authentication as a direct consequence.
  3. **The unbounded-recursion class still needs a structural fix, not
     another per-site issue.** Nineteen tracked instances all share the
     same shape: a hand-written AST walker over `pg_query` or BSON input
     with no depth counter or cap. A single shared "bounded recursion"
     helper (or a `RecursionGuard`-style wrapper threaded through
     `pg_query::protobuf::Node` traversal) fixed once would close the whole
     class in one PR, versus finding and filing them one feature at a time.

---

## Actionable findings filed this cycle

None. No new CRITICAL or WARNING finding surfaced this cycle — the only
commit since the last review is that review's own report-PR merge, and the
CVE/RUSTSEC sweep found nothing newly published against any tracked,
unchanged dependency pin. `gh issue list --label security --state open` was
53 before this review and remains 53 after.
