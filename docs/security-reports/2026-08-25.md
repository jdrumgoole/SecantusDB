# Security Review — 2026-08-25

Full walk of the checklist in `scripts/security-review-prompt.md` (sections
1–13) against HEAD `4efdc64b`. A quiet window since yesterday's review
(`99c943db` → `4efdc64b`): four commits (`docs: security review 2026-08-24`
was the prior report's own merge commit), the rest a Go-gauge debugging aid
(`SECANTUS_GAUGE_KEEP_STORAGE=1`), an `$inc`/`$mul`/`$addToSet` type-rule
correctness fix in both engines, and a release blog post. None of `wire.py`,
`server.py`, `auth.py`, `commands.py`, `cursors.py`, `changestreams.py`,
`query.py` (so the tracked `$bits*` CRITICAL is untouched), the SQL-surface
files (`authz.py`, `plpgsql.py`, `planner.py`, `engine.py`, `pgauth.py`,
`pgextended.py`), `crates/secantus-wire`, `tools/build_lz4_macos.sh`, or any
`.github/workflows/*.yml` changed this window (`git diff --quiet
99c943db..4efdc64b -- <path>` confirmed empty for each, one at a time).
`uv.lock`'s diff adds `httpx2`/`httpcore2`/`httpx2-jsfetch`/`truststore` (a new
**dev-only** transitive chain, see below) and bumps `idna` 3.13 → 3.19;
`cryptography` is still pinned at `48.0.0`. The `vendor/wiredtiger` gitlink is
unchanged (`eaa187c86147b561c940e9b0e5e628f5e86d075d`).

This cycle's fresh work: (a) a full read of every genuinely new or changed
production-code surface in the diff — the `$inc`/`$mul` non-numeric-type
refusal and `$addToSet` field-order-sensitive equality fix in both engines
(`src/secantus/update.py`, `crates/secantus-core/src/update.rs`, plus their new
test files `tests/test_update_type_rules.py` and
`tests/test_mongod_differential.py`), and the Go-gauge
`SECANTUS_GAUGE_KEEP_STORAGE` debugging env var (`go_validation/runner.py`) —
all reviewed in full below; (b) independent re-reads of every prior
CRITICAL/WARNING at its cited lines against current `HEAD` — all files those
findings live in are byte-identical to yesterday, confirmed by direct `git
diff --quiet` per file, not carried forward on trust; (c) a fresh CVE/RUSTSEC
sweep (WebSearch) covering the two new dev-only packages (`httpx2`,
`httpcore2`) plus the standing watchlist (`cryptography`, `pymongo`,
`starlette`/`fastapi`, `shapely`, `s2sphere`, `python-dateutil`) — no new
advisory for anything already tracked, nothing for the two new packages
either; (d) a live PyPI check (latest `0.6.0b15`, matches `git`, zero yanks);
(e) **no new findings** this cycle.

---

## 🔴 CRITICAL (Immediate action required)

All five carried forward from 2026-08-24, re-confirmed present and unfixed
(files unchanged this window, re-read against current `HEAD`, not trusted
from the prior report):

- **NEW-since-2026-08-24, tracked as #1037** — `src/secantus/query.py:855-908`
  (`_resolve_bitmask`): the `$bitsAllSet`/`$bitsAnySet`/`$bitsAllClear`/
  `$bitsAnyClear` array-argument branch has no upper bound on the bit-position
  integer, so `mask |= 1 << bit` with an attacker-chosen multi-billion `bit`
  forces CPython to allocate and physically touch gigabytes of real memory
  while holding the GIL — wire-reachable via any `find`/`count`/`aggregate
  $match`/`update`/`delete` filter, no auth required with `--auth` off (the
  shipped default). The Rust engine already bounds this to `0..64` and does
  not have the bug — a Python-server-only divergence. `query.py` had zero
  commits this window; re-read lines 855-919 directly, mechanics unchanged.
  Issue **#1037** open.
- **#995** — `crates/secantus-wire/src/lib.rs:137-151` (`Header::body_len`): a
  `message_length = -1` wire frame sign-extends to `usize::MAX` under the
  lower-bound check and still compares as a negative `i32` under the
  upper-bound check, so both guards pass; the caller's `vec![0u8; body_len]`
  aborts the whole Rust server via `handle_alloc_error` from one
  unauthenticated 16-byte frame. Not fixed.
- **#996** — `src/secantus/sql/authz.py:42-63` (`_command_privilege`): no
  `"CREATE_PROCEDURE"`/`"DROP_PROCEDURE"`/`"CALL"` branch, all three fall
  through to `return None` (no-op authorization); `plpgsql.py:789`
  (`_run_sql`) also bypasses `_dispatch`/`authz.authorize` directly for a
  procedure body. Not fixed.
- **#909** — `src/secantus/sql/plpgsql.py`: `_open_cursor` (line 711),
  `_run_into` (line 777), `_run_sql` (line 789, shared with #996) all call
  their engine entry points directly, none route through
  `_dispatch`/`authz.authorize`. Not fixed.
- **#947** — `src/secantus/sql/planner.py:11961` builds
  `exp.Command(this="MULTIDROP_TABLE", ...)` for `DROP TABLE a, b, c`;
  `_command_privilege` has no matching branch → unauthorized multi-table
  drop. Not fixed.
- **#948** — `src/secantus/sql/engine.py:2778-2799` (`_execute_statement`)
  binds and runs a prepared statement directly at line 2799, bypassing
  `_dispatch`; no `"EXECUTE"` branch in `_command_privilege` either, so
  `EXECUTE name` is unauthorized regardless of the underlying statement's own
  privilege requirement. Not fixed.

---

## 🟠 WARNING (Address within 1 week)

All seven carried forward, re-confirmed present and unfixed; **no new**
WARNING this cycle.

- **W1 (carried forward)** — `uv.lock:525-536` still pins
  `cryptography==48.0.0`, two full patch versions behind the fix (`49.0.0`)
  for **CVE-2026-69248** (wildcard-DNS name-constraint escape in the X.509
  path verifier) and **CVE-2026-69249** (exponential path-building DoS on a
  malicious certificate chain), both re-confirmed live this cycle via
  WebSearch. Still dev-only (`trustme`'s transitive dependency, never
  imported by shipped code — re-grepped this cycle,
  `grep -rn "^import cryptography\|^from cryptography" src/` empty), still no
  countervailing reason not to bump. Not fixed. Issue **#1023** open.
- **W2 (carried forward)** — `tools/build_lz4_macos.sh` (unchanged this
  window) still downloads the `lz4/lz4` v1.10.0 release tarball over HTTPS
  with no checksum/signature verification, in the macOS wheel's
  `cibuildwheel` `before-build` step — the one path that ships to PyPI. Not
  fixed. Issue **#1024** open.
- **#976** — `src/secantus/sql/pgauth.py:65-78` (`mock_credentials`): still
  `iteration_count=4096, salt=os.urandom(16)` for a fabricated unknown-user
  verifier vs. real users' `default_iterations=15000, salt_bytes=28`
  (`auth.py:52-68`) — a user-enumeration oracle on the SQL surface's only
  auth control. Not fixed.
- **#977** — `src/secantus/query.py:1063-1071` (`_MAX_REGEX_PATTERN_LEN`,
  `_compile_regex`): still only a 1000-char pattern-length cap on the bare
  `re.compile()` call, no timeout/complexity/backtracking-depth guard. Not
  fixed. (Distinct from — and less severe than — the `$bits*` CRITICAL above:
  this operator at least has *a* cap.)
- **#978** — `.github/workflows/rust-wheels.yml`: `permissions:` appears
  exactly once (the `publish` job, line 111); the `build` (line 52) and
  `sdist` (line 90) jobs above it still declare no `permissions:` block of
  their own. Not fixed.
- **#949** — `src/secantus/sql/pgextended.py`: no cap on open portal count;
  Bind-time eager materialization for explicit-transaction read-only portals
  remains unbounded. Not fixed.
- **#910** — `.github/workflows/validate.yml`: the `pgjdbc-report` job
  (line 408) still has no `permissions:` block between it and the next one.
  Not fixed.

---

## 🟡 INFO (Best-practice recommendations)

- **I2** — `src/secantus/sql/pgauth.py:139-140` (`ScramExchange.server_final`)
  unchanged: `base64.b64decode(proof_b64)` with no `try/except`,
  `zip(..., strict=True)` with no prior length check — malformed
  client-final drops the connection via generic `except Exception` instead of
  a faithful `28P01`. Protocol-fidelity gap, not a vulnerability.
- **I3** — `.github/workflows/crash-repro.yml` still pins
  `actions/checkout@v7`, `astral-sh/setup-uv@v7`, `actions/upload-artifact@v7`
  to moving major-version tags. `workflow_dispatch`-only, `contents: read`,
  zero `secrets.*` — low blast radius. Unchanged.
- **I4** — The dead top-level `rust/` spike directory is still git-tracked
  and outside `pyproject.toml`'s sdist exclude list — harmless bytes riding
  the sdist, never compiled. Unchanged.
- **I5** — `#750`'s tracker issue (Rust `pending_async` oplog buffer size
  cap) remains open despite being verified fixed in eight consecutive prior
  reviews; nothing in this window's diff touches
  `crates/secantus-storage/src/lib.rs` at all. Left open per this run's
  "review, document, file only" scope.
- **I7 (new)** — `go_validation/runner.py`'s new
  `SECANTUS_GAUGE_KEEP_STORAGE=1` opt-out of `shutil.rmtree(storage_dir, ...)`
  is dev-only gauge tooling (excluded from the wheel/sdist entirely — the
  `go_validation/` directory is never packaged), gated by an operator-set
  environment variable rather than any wire input, and only skips deleting a
  `tempfile.mkdtemp()` directory that the same invocation created — no path
  is attacker-influenced. Noted as a positive practice (better postmortem
  debugging for the flaky-`TryNext` investigation logged in
  `tasks/backlog.md`), not a finding.

---

## 🟢 CLEAN (Passed review)

| Area | Finding |
|------|---------|
| **New: `$inc`/`$mul` non-numeric-type refusal + `$addToSet` field-order-sensitive equality (both engines)** | `src/secantus/update.py::_is_inc_numeric`/`_addtoset_equal`, `crates/secantus-core/src/update.rs::current_or_zero`/the `$addToSet` `tricky` closure, plus the new test files `tests/test_update_type_rules.py` and `tests/test_mongod_differential.py` (found via a three-way differential against real `mongod` — see the backlog entry) all read in full. This is a hardening fix, not a regression: previously `$inc`/`$mul` against a string reached `bson_add`/`bson_mul` and raised a bare uncaught `ValueError` (still caught by `dispatch`'s blanket `except Exception`, so no traceback leaked to the wire — confirmed no `$inc`/`$mul`-specific bypass of the normal error path), and against a `bool` silently computed a wrong number instead of refusing (Python's `bool` subclasses `int`). Both are now explicit `UpdateError(code=14)` refusals matching mongod. The Rust side's `current_or_zero` change is the same tightening: it now recognizes only the four genuinely-numeric BSON variants as fast-path-eligible and defers (`Fallback`) everything else, including `Boolean` explicitly (commented as deliberate). The `$addToSet` fix (`_addtoset_equal` delegating to `query._eq_numeric_aware`; the Rust `tricky` closure deferring whenever a `Document` or `Boolean` is involved) closes a real behavioural divergence (Python `==` ignores dict field order; mongod does not) but introduces no new external input surface — both changes operate purely on values already resolved from a document field via the existing `get_path`/matcher machinery, with no new parsing, no new FFI, no new `unsafe`. The Rust `Fallback` path for the new `tricky` cases matches the established pattern already reviewed in prior cycles (the standalone Rust server surfaces a generic `BadValue`, per `arith`'s existing doc comment — not a new code path). |
| **New: Go-gauge `SECANTUS_GAUGE_KEEP_STORAGE` debugging env var** | See **I7** above. |
| Dependency CVE sweep (Python) | Fresh WebSearch this cycle covered the two new dev-only packages (`httpx2`, `httpcore2` — no CVE/advisory found for either; search results returned only unrelated `urllib3`/general Python CVEs) plus the standing watchlist (`cryptography`, `pymongo`, `starlette`/`fastapi`, `shapely`, `s2sphere`, `python-dateutil`). `pymongo` search surfaced only already-known 2024-era `bson`-module CVEs (fixed well before the `>=4.6` floor; installed `4.17.0` confirmed via `uv.lock`) and the same two 2026-05-13 real-`mongod` C++ CVEs noted in prior reports (not applicable — different codebase, no FLE support to check against). `starlette` search re-confirmed CVE-2026-48710 (BadHost) is fixed at `>=1.0.1`; installed `1.3.1` per `uv.lock`, well past the fix. Nothing else returned a new advisory since yesterday. |
| Dependency CVE sweep (Rust) | No crate entered or left `crates/Cargo.lock` this window; `crates/secantus-core/src/update.rs` changed but pulls no new crate. `cargo-audit` CI gate stands as the backstop. |
| New dev dependencies (`httpx2`, `httpcore2`, `httpx2-jsfetch`, `truststore`) | Added solely to `[project.optional-dependencies].dev` (`pyproject.toml` diff confirmed — not `admin`/`opsboard`/base `dependencies`) to silence a `StarletteDeprecationWarning` in `starlette.testclient.TestClient`. `grep -rn "^import httpx2\|^from httpx2\|^import httpcore2\|^import truststore" src/` returns nothing — no shipped code imports any of the four. `idna` bumping 3.13 → 3.19 is a transitive pull of the same dev-only chain; `idna` ships no C code and has no open advisory as of this search. |
| Extras hygiene | Re-verified: base `[project.dependencies]` still carries only `pymongo`/`s2sphere`/`shapely`/`python-dateutil` (+`tomli` on <3.11); `admin`/`opsboard` still isolate FastAPI/starlette/uvicorn; `cryptography` stays dev-only; the four new httpx2-chain packages land in `dev` only. |
| Secrets scan | Full-diff sweep (`git diff 99c943db..4efdc64b`, excluding `uv.lock`/JSON) for API-key/token/password/secret/PEM/JWT/embedded-credential-URI patterns: zero hits. `git log --diff-filter=A` sweep for `.env`/`.key`/`.pem`/`secret`/`credential`-named new files this window: none. |
| Wheel/sdist build config | Unchanged this window — `pyproject.toml`'s `[tool.scikit-build]` exclude list, `CMakeLists.txt`, and the `cmake/patch_wt_*.py` patchers are untouched (confirmed by the diffstat: only the `dev` extra's dependency list and the `differential` pytest marker changed in `pyproject.toml`). No fresh empirical `unzip -l dist/*.whl` build this cycle (last done 2026-08-15, per that day's report); nothing in this window's diff would change wheel contents. |
| PyPI publish path | Live check this cycle: PyPI's current latest is `0.6.0b15` with **zero yanked releases**, matching `src/secantus/__init__.py` exactly — no out-of-band publish. `publish.yml` unchanged (not in this window's diff). |
| WT submodule pin | `vendor/wiredtiger` gitlink still `eaa187c86147b561c940e9b0e5e628f5e86d075d` (mongodb-7.0.33) — unchanged, re-confirmed by direct gitlink SHA compare. |
| GitHub Actions — full inventory | Same 13 workflow files as yesterday (`ls .github/workflows/*.yml` name list matches exactly) — no new workflow this window, and `git diff --stat 99c943db..4efdc64b -- .github/` is empty. `security-review.yml` itself unchanged: `permissions:` still scoped to `contents: write`/`pull-requests: write`/`issues: write`/`actions: read`, no `id-token: write`. |
| Mongo wire-protocol parsing (`wire.py`) | Unchanged since 2026-08-19's full read; not in this window's diff. |
| Rust wire protocol (`crates/secantus-wire`) | `src/` unchanged (`#995` re-confirmed above); not in this window's diff. |
| Rust unsafe/panic audit (`crates/**`) | Only `crates/secantus-core/src/update.rs` changed this window (+37/-5 lines, reviewed in full above) — confirmed by grepping the diff hunk directly for the token `unsafe`: zero matches, and no new `panic!`/`unwrap()`/`expect()` on wire-derived data (the one new `.expect("just matched")` in `current_or_zero` operates on a `get_path` result whose `Some`-ness and variant were just established by the immediately preceding `match` arm on the *same* document — not attacker-controllable to violate, and no mutation occurs between the two calls). Per-crate `unsafe` counts unchanged from prior sweeps (`secantus-wt`: 68, `secantus-storage`: 3, `secantusdb`: 1, everything else: 0). |
| Storage / WiredTiger FFI | `storage.py` unchanged this window; not in this window's diff. |
| Mongo SCRAM-SHA-256/1 | `auth.py` unchanged; not in this window's diff. |
| MONGODB-X509 / mTLS | `server.py` unchanged; not in this window's diff. |
| `--auth` gating (Mongo) | `commands.py` unchanged; not in this window's diff. `_NO_PRIVILEGE_COMMANDS` still minimal. |
| `getMore` cursor ownership | `commands.py`/`cursors.py` unchanged. |
| `system.users` / `system.version` | `storage.py` untouched this window. |
| Credential/token logging | No new `log`/`print` call near password/secret/SCRAM handling introduced by this window's four commits (the one new `print` in `go_validation/runner.py` only echoes a temp-directory path, never a credential). |
| Admin UI / Ops Board | No commit touched the admin-UI source this window. |
| No `eval`/`exec`/`compile`/`__import__`-as-injection | None of this window's four commits touch a query/pipeline evaluation path with a new interpreter surface. |
| Cursor cap / TTL | `cursors.py` unchanged; `DEFAULT_MAX_CURSORS = 10_000` + 600s idle-TTL pruning still in place. |
| Change-stream `awaitData` bound / oplog retention | `storage.py`/`changestreams.py` unchanged this window. |
| Network exposure & defaults | `server.py` unchanged this window — `host="127.0.0.1"` remains the constructor default. |
| `tests/test_mongod_differential.py` (new file) | Spawns `mongod` via `subprocess.Popen([MONGOD, "--port", str(port), "--dbpath", tmp, "--quiet"], ...)` — a list-form call (no `shell=True`), all arguments either a `which`-resolved binary path, a kernel-assigned free port, or a `tempfile.mkdtemp()` path; no string interpolation of any external input. Dev-only, skipped when no `mongod` binary is on `PATH`, and (like all of `tests/`) not part of `wheel.packages` so it never ships. |

---

## 📊 Summary

- **Date of review:** 2026-08-25.
- **SecantusDB version reviewed:** Python server `0.6.0b15`
  (`src/secantus/__init__.py`); Rust server `0.5.3-beta.162` (all 12
  `crates/*/Cargo.toml`, lockstep-confirmed) — no release cut since
  2026-08-24.
- **Total issues by severity:** 5 CRITICAL (all carried forward from
  2026-08-24: **#1037** (`$bits*`), **#995, #996, #909, #947, #948** — zero
  new) · 7 WARNING (**W1, W2, #976, #977, #978, #949, #910 — all carried
  forward, all re-confirmed present and unfixed, zero new**) · 6 INFO (I2–I5
  re-derived unchanged, I7 new — a positive note on the Go-gauge debugging
  aid) · large CLEAN inventory, including every genuinely new or changed
  production-code surface in this window's four commits.
- **Comparison with previous report (2026-08-24):** A quieter window than
  2026-08-24's ten-commit day — four commits, no wire/auth/dispatch/SQL-surface
  files touched, and the one file behind yesterday's new CRITICAL
  (`query.py`) had zero commits this window. Every carried-forward CRITICAL
  and WARNING was re-verified by direct file read against current `HEAD` (not
  trusted from the prior report) and all twelve are unchanged in substance and
  line location. The two genuinely new production-code surfaces this window —
  the `$inc`/`$mul`/`$addToSet` type-rule hardening (both engines) and the
  Go-gauge debugging env var — were both read in full for the first time this
  cycle and found sound; the update-operator fix is itself a security-adjacent
  hardening (closing a silent-wrong-write and an uncaught-exception path), not
  a regression.
- **Top 3 priorities for the team (unchanged from yesterday — nothing moved):**
  1. **#1037 — bound the `$bits*` bit-position argument in the Python query
     engine.** `src/secantus/query.py:855-908`'s `_resolve_bitmask` has no
     upper bound on an attacker-supplied bit position, so a single ordinary
     `find` filter can force the interpreter to allocate and physically touch
     several gigabytes of real memory while holding the GIL — no
     authentication or elevated privilege required. The Rust engine already
     does this correctly (bounds every position to `0..64`); the fix is to
     make the Python engine match it. Now one full day old with zero movement.
  2. **#995 — fix the Rust `body_len()` bounds check.** Unauthenticated,
     single-frame, full-process DoS on the Rust server; the fix shape is
     already known and nothing has landed on it in seven full review cycles.
  3. **Build the systemic fix for the SQL-authz dispatch-gap bug class**
     (#909, #947, #948, #996 all share one root cause: statement kinds
     parsed as `exp.Command` or executed via an embedded/synthetic path that
     never reaches `_dispatch`/`authz.authorize`). Every report since
     2026-08-16 has flagged this; it is now ten days old with zero movement.
