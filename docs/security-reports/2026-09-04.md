# Security Review — 2026-09-04

Full walk of `scripts/security-review-prompt.md` (sections 1–13) against HEAD
`c1648ad8`. The diff since the prior report's HEAD (`b88bf71f`, 2026-09-03) is
124 files / +14,587 / −361 by line count, but almost entirely SQL/PostgreSQL-
wire test corpora and probe tooling (`tests/test_sql_sweep_*.py`,
`tests/test_rust_pgserver_*.py`, `tools/probes/pg_corpora/**`,
`tools/probes/pg_differential.py`). **Zero lines changed** in
`src/secantus/wire.py`, `server.py`, `auth.py`, `commands.py`, or any
`crates/secantus-wire` / `secantus-server` / `secantus-wt` source file — the
checklist's highest-value surfaces are unchanged for the third consecutive
cycle. The in-scope diff is: `src/secantus/storage.py` (+21, a NaN-vs-partial-
index gate), `crates/secantus-storage/src/lib.rs` (+17, the same fix ported to
Rust), `src/secantus/expressions.py` (+304/−72, `$type`/signed-zero fixes),
`crates/secantus-core/src/{decimal,expressions,query,update}.rs` (the Rust
side of the same expression fixes), two new `Cargo.lock` files, and a 2-line
`.github/workflows/test.yml` addition.

Methodology, unchanged from the last two cycles: (a) re-verify every open
CRITICAL/WARNING by reading the exact current lines rather than trusting the
prior report's line numbers or word; (b) diff the actual code change against
the checklist's in-scope surfaces to catch anything genuinely new; (c) re-run
the cheap, high-signal sweeps fresh every cycle regardless of diff size:
secrets scan, WT submodule pin, PyPI publish/version state, the
`secantus-core` PyPI-registration check, the admin-UI `| safe` census. Same
scope carve-out as the last two cycles: this run did not re-derive the
SQL/PG-wire subsystem's standing findings from first principles (still not
named in the prompt's checklist); it cross-checks the tracker to confirm they
remain open.

---

## 🔴 CRITICAL (Immediate action required)

All three unchanged from 2026-09-03 — re-verified by reading current source,
not carried on trust. All are reachable from an ordinary (unauthenticated, for
the first two) wire client:

- **#995** — `crates/secantus-wire/src/lib.rs:136-150` (`Header::body_len`).
  Re-read this cycle, byte-identical to yesterday:
  ```rust
  pub fn body_len(&self) -> Result<usize, WireError> {
      if (self.message_length as usize) < HEADER_SIZE { ... }
      if self.message_length > MAX_MESSAGE_SIZE { ... }
      Ok(self.message_length as usize - HEADER_SIZE)
  }
  ```
  `message_length` is `i32`. A header with `message_length = -1` sign-extends
  under `as usize` to `usize::MAX` in the first check (comparison becomes
  `usize::MAX < 16` → false, guard doesn't fire), and the second check
  compares the still-signed `i32` (`-1 > MAX_MESSAGE_SIZE` → false) — both
  guards let a negative length through, and the function returns
  `Ok(usize::MAX - 16)`. `crates/secantus-server/src/lib.rs` (unchanged this
  cycle) still turns that into `vec![0u8; body_len]`, an unrecoverable
  allocator abort — not `catch_unwind`-able, kills the whole process — from
  one unauthenticated 16-byte frame. **Not fixed.**

- **#1160** — `src/secantus/commands.py`. Re-confirmed this cycle:
  `"bulkWrite": _bulk_write` is registered in the dispatch table (line 8608)
  but has no entry in `_COMMAND_ACTIONS` (span checked fresh this cycle,
  8690-8783) and is not in `_NO_PRIVILEGE_COMMANDS` (8783-8806) either. The
  privilege gate at `commands.py:9613-9614` (`info = _COMMAND_ACTIONS.get(name);
  if info is not None: ...`) is still fail-*open*: a command absent from the
  table is silently exempted from RBAC rather than denied. `bulkWrite` still
  bypasses authorization entirely under `--auth`. **Not fixed.**

- **#977** — `src/secantus/query.py:1275` (`_MAX_REGEX_PATTERN_LEN = 1000`),
  re-read this cycle at the same line. Still a length cap only, no
  backtracking-depth or match-time budget. Per the 2026-09-02 live-verified
  finding (a pathological `(a+)+$`-shaped pattern starves the whole process
  because CPython's `_sre` engine never releases the GIL mid-match — confirmed
  to hang an unrelated second connection's `ping`, not just the attacking
  connection), this remains CRITICAL rather than a single-connection-cost
  WARNING. **Not fixed.**

---

## 🟠 WARNING (Address within 1 week)

All six 2026-09-02/03 findings re-verified present, unchanged, at current
HEAD (each re-read from source this cycle):

- **`killSessions`/`killAllSessions`/`killAllSessionsByPattern` still bypass
  RBAC** (issue **#1226**) — the three handlers (`commands.py:8580-8582`)
  remain absent from both `_COMMAND_ACTIONS` (grepped the 8690-8783 span
  fresh, zero hits) and `_NO_PRIVILEGE_COMMANDS`. Same fail-open gate as
  `bulkWrite` above. **Not fixed.**
- **Rust `secantusd-rs` background maintenance threads still have no panic
  isolation** (issue **#1227**) — `crates/secantusdb/src/main.rs:287`'s
  `spawn_interval` is still a bare `thread::spawn` with no `catch_unwind`;
  `grep -rn "^panic" crates/*/Cargo.toml Cargo.toml` still zero hits (no
  `panic = "abort"` anywhere). **Not fixed.**
- **The `#139` namespace-NUL guard still doesn't cover `renameCollection`'s
  `to` field** (issue **#1228**) — `crates/secantus-commands/src/admin.rs:1295`
  still calls `argtypes::require_required_string(doc, "to", ...)` with no NUL
  check; `grep -n "NUL" crates/secantus-commands/src/aggregate.rs` still zero
  hits — `$out`/`$merge`/`$lookup`/`$graphLookup`/`$unionWith` unguarded.
  **Not fixed.**
- **`.github/workflows/publish.yml`'s `verify_tag`/`build_wheels`/
  `build_sdist` jobs still have no `permissions:` block** (issue **#1229**)
  — re-read the job list this cycle: only the terminal `publish` job (line
  177) declares `permissions:`; the three jobs ahead of it still inherit the
  repo/org default `GITHUB_TOKEN` grant. **Not fixed.**
- **PyPI sdist packaging gaps** (issue **#1230**) — `pyproject.toml`'s
  `[tool.scikit-build] sdist.exclude` still has no `vendor/mongo-rust-driver/**`
  or `rust_validation/**` entries (`grep -n "mongo-rust-driver\|rust_validation"
  pyproject.toml` → zero matches, re-run this cycle). **Not fixed.**
- **`secantus-core` PyPI dependency-confusion exposure** (issue **#1231**)
  — `pyproject.toml:138` still pins `"secantus-core==0.5.2b15"` as a plain
  PyPI dependency of the `rust` extra. Re-queried PyPI fresh this cycle
  (`pypi.org/pypi/secantus-core/json`): still `404 Not Found` — the name
  remains unregistered and available for anyone to claim. **Not fixed.**

**No new WARNING-tier findings surfaced in this cycle's in-scope diff.** The
NaN-vs-partial-index fix in `storage.py`/`secantus-storage/src/lib.rs` is a
**correctness/data-integrity fix, not a security regression** — it closes a
silent-data-loss bug (`find({b: NaN})` returning nothing when a NaN-valued
partial index was in play) that CLAUDE.md already documents as found-and-fixed
2026-09-03; re-read both diffs this cycle and confirmed the new `_is_nan_value`
/ `is_nan_bson` gate is correctly placed before the type-bracket check on both
servers, with no new attacker-reachable code path introduced. The
`expressions.py` / `crates/secantus-core` diff (`$type`, signed-zero,
`$round`/`$trunc` integer-arithmetic fixes) was scanned for the two bug
classes that would be new-and-in-scope even under the narrower checklist
reading: no `eval`/`exec`/`compile`/`__import__`/`os.system`/`subprocess`
introduced anywhere, and the two new `.unwrap()`/`.expect()` call sites in
`crates/secantus-core/src/expressions.rs` (`op_round`, `op_trunc`) are both
inside an `if matches!(n, Bson::Int32(_) | Bson::Int64(_))` guard (or the
equivalent prior match arm) that guarantees the precondition — not new
attacker-reachable panics. The two new `Cargo.lock` files
(`crates/secantus-server-py`, `crates/secantus-storage-adapter`, tracked by
commit `9274dfa9`) pin `tinyvec` to the verified-good `1.12.0` rather than
leaving those two WT-linked crates to re-resolve un-pinned — a supply-chain
*improvement*, not a new risk; see CLEAN below.

**Reclassified from a prior report's INFO tier, left at INFO (not
escalated), unchanged reasoning:**

- `.github/workflows/crash-repro.yml` still pins `actions/checkout@v7` /
  `astral-sh/setup-uv@v7` / `actions/upload-artifact@v7` to moving tags.
  `workflow_dispatch`-only trigger, `contents: read`, no `secrets.*` —
  blast radius bounded to something only a maintainer can already trigger.

---

## 🟡 INFO (Best-practice recommendations)

All carried forward, re-checked where cheap to do so this cycle:

- `.claude/settings.json` (hardcoded maintainer machine path,
  `/Users/jdrumgoole/...`) still ships in the sdist per the unchanged
  `sdist.exclude` config.
- `validation_summary/` still ships in the sdist for the same reason.
- `[tool.cibuildwheel.linux] before-build`'s `curl https://sh.rustup.rs |
  sh` bootstrap is unchanged — HTTPS, official installer, not
  checksum-pinned.
- `crates/pgo/_secantus_server.profdata.tar.gz` — unchanged, clean
  provenance.
- `publish.yml`'s `verify_tag` still checks only `pyproject.toml`'s version,
  not `src/secantus/__init__.py`'s — both independently confirmed at
  `0.6.0b16` this cycle, still in sync via `tasks.py`'s
  `_bump_version_files()`.
- `cryptography==48.0.0` (`uv.lock`) — re-confirmed pinned, still behind the
  named CVEs fixed in 49.0.0/50.0.0, still tracked as **#1023** (open),
  still reachable only via `trustme`'s test-only transitive dependency, not
  shipped runtime code.
- `starlette==1.3.1` — re-confirmed pinned exactly at the CVE-2026-54283 fix
  floor with zero version margin (latest 1.6.0); `uvicorn==0.46.0` unchanged.
  Both `admin`/`opsboard`-extra-only, not base install.
- `.gitignore` — re-confirmed: covers `.env`/`*.pem`/`*.key`/`.coverage`/
  `htmlcov/`/`.venv`/`.venv/`; `git ls-files | grep -E '/target/'` still
  returns zero tracked files — latent, not live.
- **New this cycle:** two new `Cargo.lock` files landed for
  `secantus-server-py` and `secantus-storage-adapter` (previously
  gitignored, per `CLAUDE.md`'s note that WT-linked crates are excluded from
  the clean workspace). They now pin the same dependency graph the other
  four WT-linked crates already lock, closing an un-pinned-resolution gap
  (a `tinyvec 1.13.0` re-resolve broke the build repo-wide on 2026-09-03 with
  no dependency change in the triggering commit). Net posture improvement,
  not a new risk — noted here rather than CLEAN only because it's new state
  worth a future cycle's attention if these locks drift from the other four.

---

## 🟢 CLEAN (Passed review)

Re-verified this cycle (not carried on trust):

| Area | Finding |
|---|---|
| Hardcoded secrets scan | Fresh sweep across `src/`, `crates/`, `tests/`, `website/`, `docs/` for API-key/token/password/secret literal patterns, `AKIA`/`sk-`/embedded-credential `mongodb://user:pass@` URIs, PEM headers, JWTs, plus the new probe/corpus files added this cycle (`tools/probes/pg_differential.py`, `tools/probes/pg_corpora/**`) — zero real hits. Every `mongodb://user:pass@`-shaped string is a test fixture or docs placeholder with an obviously fake password (`alice:s3cret`, `root:rootpw`, `scramuser:hunter2`). `git ls-files | grep -iE '\.env\|\.key\|\.pem\|\.p12\|\.pfx\|secret\|credential'` returns zero tracked files. |
| WT submodule pin | Gitlink `eaa187c86147b561c940e9b0e5e628f5e86d075d` unchanged from 2026-09-03; submodule not checked out in this sandbox (expected — not a finding). |
| PyPI publish/version state | Live query: `pypi.org/pypi/SecantusDB/json` latest version `0.6.0b16` matches HEAD's `pyproject.toml` + `src/secantus/__init__.py` + the latest git tag exactly; zero yanked releases. No out-of-band publish. |
| `secantus-core` PyPI registration | Re-queried fresh: `pypi.org/pypi/secantus-core/json` still `404` — confirms the WARNING above is unchanged, not worsened. |
| New WT-linked lockfiles (`secantus-server-py`, `secantus-storage-adapter`) | Both pin `tinyvec = "1.12.0"`, the version explicitly verified clean in commit `9274dfa9`'s message (builds clean, 127 tests passing) — consistent with the other four WT-linked crates' existing locks, no stray/unverified dependency introduced. |
| GitHub Actions workflow inventory | 13 workflow files present, same count/set as 2026-09-03; only `test.yml` changed (2-line addition of two new test files to an existing `pytest` invocation's arg list — no `run:` injection surface, no permissions/secrets/pinning change). |
| Admin UI Jinja autoescape (`\| safe` census) | Still the single `geo.html:51` use, still backed by `extras.py`'s pre-escaping `_json_for_script()` — unchanged from prior cycles' traced-and-cleared verdict. |
| Wire-protocol / storage / auth / dispatch / resource-exhaustion / network-default / Rust-`unsafe`/panic census / GitHub-Actions pinning-injection-permissions-secrets-cache / wheel-content-scoping / extras-hygiene | Zero lines changed in `wire.py`, `server.py`, `auth.py`, `commands.py`'s dispatch machinery, `crates/secantus-wire`, `crates/secantus-server`, `crates/secantus-wt`, `CMakeLists.txt`, `[tool.scikit-build]` wheel packaging since 2026-09-02's full line-by-line review; that review's CLEAN verdicts stand un-re-derived for identical bytes. `storage.py` / `crates/secantus-storage` did change (NaN gate) — reviewed fresh above, correctness fix confirmed to introduce no new attacker-reachable path. |

---

## 📊 Summary

- **Date of review:** 2026-09-04.
- **SecantusDB version reviewed:** Python server `0.6.0b16`
  (`src/secantus/__init__.py`); Rust Mongo server `0.5.3-beta.163` (all 12
  lockstep `crates/*/Cargo.toml`, unchanged); Rust PostgreSQL server crates
  (`secantus-pgserver`/`-pgplan`/`-pgcatalog`) independently versioned
  `0.1.0-beta.0` (not covered this cycle — see the scope note).
- **Total issues by severity (this cycle's checklist scope):** 3 CRITICAL
  (all carried forward, re-verified unchanged — `#995`, `#1160`, `#977`) ·
  6 WARNING (all carried forward, re-verified unchanged — `#1226`–`#1231`)
  plus 15 WARNING-tier items cited from the standing tracker but outside
  this cycle's checklist scope (SQL/PG-wire RBAC-dispatch class, Rust
  unbounded-recursion class, PG-server hardening gaps, two more
  missing-`permissions:` workflow jobs, one unchecksummed build-time
  download) · 8 INFO (7 carried forward + 1 new: the two newly-tracked
  WT-linked `Cargo.lock` files) · large CLEAN inventory, the highest-value
  sections re-verified clean, plus a fresh review of this cycle's actual
  in-scope diff (NaN partial-index gate, expression-engine fixes, lockfile
  additions).
- **Comparison with the previous report (2026-09-03):** No findings were
  fixed. No new CRITICAL/WARNING surfaced. The NaN partial-index fix that
  landed in this diff closes a *correctness* bug already tracked in
  `CLAUDE.md`, not a security finding — it was reviewed and found to
  introduce no new attacker-reachable path. All nine 2026-09-03 findings
  (3 CRITICAL + 6 WARNING) reproduce identically at current HEAD.
- **Top 3 priorities for the team:** unchanged from the last two cycles,
  because nothing changed on the surfaces they name:
  1. **Fix the two whole-server-DoS-from-one-request bugs** — `#995`'s Rust
     `body_len` cast-before-check (reorder: check the sign/lower-bound on
     the raw `i32` before casting to `usize`) and `#977`'s regex
     GIL-starvation (add a backtracking-safe engine or a match-time
     budget for `$regex`). Both are still a single crafted request away
     from taking the entire server offline for every client, unauthenticated
     in the first case.
  2. **Close the recurring "command missing from `_COMMAND_ACTIONS` silently
     skips RBAC" bug class systemically**, not per-command — four confirmed
     live instances across three report cycles (`bulkWrite`/`#1160`,
     `killSessions`/`killAllSessions`/`killAllSessionsByPattern`/`#1226`),
     on top of two already fixed (`getParameter`, lowercase `dbstats`).
     Invert `commands.py:9613`'s check to fail-closed: unknown command +
     auth on + not explicitly no-privilege → deny.
  3. **Fold the SQL/PG-wire subsystem into `scripts/security-review-
     prompt.md` explicitly.** Three cycles running now, this subsystem has
     received the largest code changes in the repo (10K+ lines the last two
     cycles combined — `AT TIME ZONE`, JSON path functions, `COPY` options,
     window functions, notification delivery, cursors) while remaining
     entirely outside the checklist's named scope, leaning on institutional
     memory (five RBAC-dispatch issues, four Rust-recursion issues, PG-auth
     and PG-hardening issues) rather than construction to keep it covered.

---

## Actionable findings filed this cycle

None. Cross-checked `gh issue list --label security --state open` (31 open)
before concluding: every CRITICAL/WARNING finding in this report already has
an open tracking issue from a prior cycle (`#995`, `#1160`, `#977`, `#1226`,
`#1227`, `#1228`, `#1229`, `#1230`, `#1231`), each re-verified present and
unfixed at current HEAD rather than re-filed. No new finding required a new
issue this cycle.
