# Security Review — 2026-08-26

Full walk of the checklist in `scripts/security-review-prompt.md` (sections
1–13) against HEAD `5a81757f`. Five commits since yesterday's review
(`4efdc64b` → `5a81757f`, `docs: security review 2026-08-25` was the prior
report's own merge commit): an exact Decimal128 arithmetic rewrite for
`$inc`/`$mul`/`$sum`/`$avg` on both engines (#1039), a fix so an *explicit*
`maxTimeMS: 0` on a change-stream `getMore` returns immediately instead of
blocking (#1041), an `$inc`/`$mul` error-message wording fix to match mongod's
`{_id: …}` rendering (#1044), and a website-only revert of the driver-panel
numbers (#1043). None of `wire.py`, `server.py`, `storage.py`, `auth.py`,
`query.py` (so the tracked `$bits*` CRITICAL is untouched), `cursors.py`,
`changestreams.py`, the SQL-surface files (`authz.py`, `plpgsql.py`,
`planner.py`, `engine.py`, `pgauth.py`, `pgextended.py`), `crates/secantus-wire`,
or any `.github/workflows/*.yml` changed this window — confirmed one file at a
time with `git diff --quiet 4efdc64b..HEAD -- <path>`, not assumed. `uv.lock`
and every `Cargo.lock` are byte-identical to yesterday (no dependency moved);
the `vendor/wiredtiger` gitlink is unchanged
(`eaa187c86147b561c940e9b0e5e628f5e86d075d`, re-confirmed via
`git ls-tree HEAD -- vendor/wiredtiger`, not the submodule's own possibly-stale
checkout).

This cycle's fresh work: (a) a full read of every genuinely new or changed
production-code surface in the five-commit diff — `src/secantus/numerics.py`'s
new `decimal_from_double`/`decimal_from_double_exact`/`bson_sum` (the
15-significant-digit vs. exact-binary double→decimal128 conversion rules
`$inc`/`$mul` and `$sum`/`$avg` each need), the new
`crates/secantus-core/src/decimal.rs` (708 lines, a from-scratch exact
decimal128 arithmetic module replacing the prior "Decimal128 always defers to
Python" behaviour), the `update.rs`/`storage-adapter`/`storage` wiring that
lets the standalone Rust server name a non-numeric `$inc`/`$mul` as
`TypeMismatch` (14) instead of a generic `BadValue`, `commands.py`'s
`_get_more` non-blocking-poll fix, and the `update.py`/`aggregate.py`/
`expressions.py` call-site + error-message changes that ride along with both —
all reviewed in full below; (b) independent re-reads of every prior
CRITICAL/WARNING at its cited lines against current `HEAD` — all twelve files
those findings live in are byte-identical to yesterday, confirmed by direct
`git diff --quiet` per file, not carried forward on trust; (c) a fresh CVE
sweep (WebSearch) re-confirming `cryptography`'s two named CVEs
(CVE-2026-69248, CVE-2026-69249) are still open at the installed `48.0.0`
(fixed in `49.0.0`) and that Starlette's BadHost (CVE-2026-48710) remains
fixed at the installed `1.3.1`; no new advisory surfaced for `pymongo`,
`shapely`, `s2sphere`, or `python-dateutil`; (d) a live PyPI check (latest
`0.6.0b15`, matches `git`, zero yanks); (e) a live `gh issue list --label
security --state open` cross-check confirming all twelve tracked
CRITICAL/WARNING issues (plus the long-standing INFO tracker #750) are still
open with unchanged titles — no re-filing needed; (f) **no new findings** this
cycle.

---

## 🔴 CRITICAL (Immediate action required)

All five carried forward from 2026-08-25, re-confirmed present and unfixed
(files unchanged this window, re-read against current `HEAD`, not trusted
from the prior report):

- **#1037** — `src/secantus/query.py:855-908` (`_resolve_bitmask`): the
  `$bitsAllSet`/`$bitsAnySet`/`$bitsAllClear`/`$bitsAnyClear` array-argument
  branch has no upper bound on the bit-position integer, so `mask |= 1 << bit`
  with an attacker-chosen multi-billion `bit` forces CPython to allocate and
  physically touch gigabytes of real memory while holding the GIL —
  wire-reachable via any `find`/`count`/`aggregate $match`/`update`/`delete`
  filter, no auth required with `--auth` off (the shipped default). The Rust
  engine already bounds this to `0..64` and does not have the bug — a
  Python-server-only divergence. `query.py` had zero commits this window;
  re-read lines 855-919 directly, mechanics unchanged. Issue **#1037** open.
- **#995** — `crates/secantus-wire/src/lib.rs:137-151` (`Header::body_len`): a
  `message_length = -1` wire frame sign-extends to `usize::MAX` under the
  lower-bound check and still compares as a negative `i32` under the
  upper-bound check, so both guards pass; the caller's `vec![0u8; body_len]`
  aborts the whole Rust server via `handle_alloc_error` from one
  unauthenticated 16-byte frame. Not fixed.
- **#996** — `src/secantus/sql/authz.py:42-63` (`_command_privilege`): no
  `"CREATE_PROCEDURE"`/`"DROP_PROCEDURE"`/`"CALL"` branch, all three fall
  through to `return None` (no-op authorization); `plpgsql.py:789`
  (`_run_sql`) also bypasses `_dispatch`/`authz.authorize` directly for a
  procedure body. Not fixed.
- **#909** — `src/secantus/sql/plpgsql.py`: `_open_cursor` (line 711),
  `_run_into` (line 777), `_run_sql` (line 789, shared with #996) all call
  their engine entry points directly, none route through
  `_dispatch`/`authz.authorize`. Not fixed.
- **#947** — `src/secantus/sql/planner.py:11961` builds
  `exp.Command(this="MULTIDROP_TABLE", ...)` for `DROP TABLE a, b, c`;
  `_command_privilege` has no matching branch → unauthorized multi-table
  drop. Not fixed.
- **#948** — `src/secantus/sql/engine.py:2778-2799` (`_execute_statement`)
  binds and runs a prepared statement directly at line 2799, bypassing
  `_dispatch`; no `"EXECUTE"` branch in `_command_privilege` either, so
  `EXECUTE name` is unauthorized regardless of the underlying statement's own
  privilege requirement. Not fixed.

---

## 🟠 WARNING (Address within 1 week)

All seven carried forward, re-confirmed present and unfixed; **no new**
WARNING this cycle.

- **W1 (carried forward)** — `uv.lock:525-536` still pins
  `cryptography==48.0.0`, two full patch versions behind the fix (`49.0.0`)
  for **CVE-2026-69248** (wildcard-DNS name-constraint escape in the X.509
  path verifier) and **CVE-2026-69249** (exponential path-building DoS on a
  malicious certificate chain), both re-confirmed live this cycle via
  WebSearch. Still dev-only (`trustme`'s transitive dependency, never
  imported by shipped code — re-grepped this cycle,
  `grep -rn "^import cryptography\|^from cryptography" src/` empty), still no
  countervailing reason not to bump. Not fixed. Issue **#1023** open.
- **W2 (carried forward)** — `tools/build_lz4_macos.sh` (unchanged this
  window) still downloads the `lz4/lz4` v1.10.0 release tarball over HTTPS
  with no checksum/signature verification, in the macOS wheel's
  `cibuildwheel` `before-build` step — the one path that ships to PyPI. Not
  fixed. Issue **#1024** open.
- **#976** — `src/secantus/sql/pgauth.py:65-78` (`mock_credentials`): still
  `iteration_count=4096, salt=os.urandom(16)` for a fabricated unknown-user
  verifier vs. real users' `default_iterations=15000, salt_bytes=28`
  (`auth.py:52-68`) — a user-enumeration oracle on the SQL surface's only
  auth control. Not fixed.
- **#977** — `src/secantus/query.py:1063-1071` (`_MAX_REGEX_PATTERN_LEN`,
  `_compile_regex`): still only a 1000-char pattern-length cap on the bare
  `re.compile()` call, no timeout/complexity/backtracking-depth guard. Not
  fixed. (Distinct from — and less severe than — the `$bits*` CRITICAL above:
  this operator at least has *a* cap.)
- **#978** — `.github/workflows/rust-wheels.yml`: `permissions:` appears
  exactly once (the `publish` job, line 111); the `build` (line 52) and
  `sdist` (line 90) jobs above it still declare no `permissions:` block of
  their own. Not fixed.
- **#949** — `src/secantus/sql/pgextended.py`: no cap on open portal count;
  Bind-time eager materialization for explicit-transaction read-only portals
  remains unbounded. Not fixed.
- **#910** — `.github/workflows/validate.yml`: the `pgjdbc-report` job
  (line 408) still has no `permissions:` block between it and the next one.
  Not fixed.

---

## 🟡 INFO (Best-practice recommendations)

- **I2** — `src/secantus/sql/pgauth.py:139-140` (`ScramExchange.server_final`)
  unchanged: `base64.b64decode(proof_b64)` with no `try/except`,
  `zip(..., strict=True)` with no prior length check — malformed
  client-final drops the connection via generic `except Exception` instead of
  a faithful `28P01`. Protocol-fidelity gap, not a vulnerability.
- **I3** — `.github/workflows/crash-repro.yml` still pins
  `actions/checkout@v7`, `astral-sh/setup-uv@v7`, `actions/upload-artifact@v7`
  to moving major-version tags. `workflow_dispatch`-only, `contents: read`,
  zero `secrets.*` — low blast radius. Unchanged.
- **I4** — The dead top-level `rust/` spike directory is still git-tracked
  and outside `pyproject.toml`'s sdist exclude list — harmless bytes riding
  the sdist, never compiled. Unchanged.
- **I5** — `#750`'s tracker issue (Rust `pending_async` oplog buffer size
  cap) remains open despite being verified fixed in nine consecutive prior
  reviews; nothing in this window's diff touches
  `crates/secantus-storage/src/lib.rs`'s buffer logic (the one change to that
  file this window is the new `UpdateTypeMismatch` error-mapping arm, see
  CLEAN below — unrelated code path). Left open per this run's "review,
  document, file only" scope.
- **I8 (new)** — `crates/secantus-core/src/decimal.rs`'s `WORK_DIGITS`
  constant (12,400) replaces a prior, narrower 400-digit working-buffer bound
  that a differential fuzz run found insufficient for a genuine edge case (a
  denormal `f64` like `5e-324` combined with a decimal near `E+25` needs 401
  digits to exactly align). The new bound is sized from the *fixed* exponent
  ranges of `f64` and `Decimal128` (both bounded BSON/IEEE types, not
  attacker-extensible), and every use site (`decimal.rs:320`, `:413`) checks
  `width > WORK_DIGITS` / `shift > WORK_DIGITS` and returns `None` (→
  `Fallback`) rather than allocating past the bound — the same shape as the
  guard the `$bits*` CRITICAL above is missing. Noted as a positive practice
  (a previously-fixed near-miss handled the *right* way this time), not a
  finding.

---

## 🟢 CLEAN (Passed review)

| Area | Finding |
|------|---------|
| **New: exact Decimal128 arithmetic (`$inc`/`$mul`/`$sum`/`$avg`, both engines)** | `src/secantus/numerics.py`'s new `decimal_from_double` (15-significant-digit conversion, the `$inc`/`$mul`/`$toDecimal` rule) and `decimal_from_double_exact` (exact-binary conversion, the `$sum`/`$avg` accumulator rule) both run inside a fixed `decimal.Context(prec=34, rounding=ROUND_HALF_EVEN)` — bounded precision, no unbounded string/digit growth from a crafted double (every finite `f64` has a fixed-length exact decimal expansion, capped well under the context's working room). `crates/secantus-core/src/decimal.rs` (new, 708 lines) is the Rust port: hand-rolled sign/coefficient/exponent arithmetic bounded by the `WORK_DIGITS = 12_400` guard discussed in **I8** above — every call site defers (`Fallback`) rather than growing past it. No `unsafe` anywhere in the file (`grep -n "unsafe" decimal.rs` — zero hits); the only `.unwrap()`/`.expect()` calls are inside `#[cfg(test)] mod tests` (confirmed by line-range cross-check against the `mod tests` boundary at line 535), none reachable from wire input. The `update.rs` wiring (`arith_type_error`, `is_arith_numeric`, `render_scalar`) that lets the type-mismatch error surface as mongod's `TypeMismatch` (14) on the standalone Rust server builds its error *message* by `format!`-interpolating the operand/path/doc `_id` — a string returned to the client in an error reply, never executed, matching the existing `src/secantus/update.py::_render_doc_id`/`_render_bson_scalar` pattern reviewed in prior cycles. `storage-adapter/src/lib.rs`'s new `WtError::UpdateTypeMismatch` arm and `storage/src/lib.rs`'s new match arm are pure error-code routing, no new I/O or FFI. |
| **New: `getMore` explicit `maxTimeMS: 0` non-blocking fix** | `src/secantus/commands.py::_get_more`'s new `no_wait = "maxTimeMS" in doc and max_time_ms <= 0` only widens an existing bound-wait condition to sometimes skip the wait entirely (never lengthens or removes a bound) — the awaitData block this touches was already covered by prior reviews' "bounded to 1s default" finding (§7) and remains bounded; this change can only make a getMore return *faster*, not add a new blocking or resource-growth path. `tests/test_getmore_maxtimems_zero.py` (new, 125 lines) drives the fix via a real `SecantusDBServer(port=0, ...)` fixture, no new subprocess/file/network surface. |
| **New: `$inc`/`$mul` error-message `{_id: …}` rendering fix** | `src/secantus/update.py`'s new `_render_doc_id` and the `ObjectId` branch added to `_render_bson_scalar` only change what a `TypeMismatch` error message's text looks like (to match mongod's wording); the underlying refusal-vs-apply decision (`_is_inc_numeric`) is untouched. No new input path — operates on `doc["_id"]`, already resolved from the document being updated. |
| Dependency CVE sweep (Python) | Fresh WebSearch this cycle re-confirmed CVE-2026-69248/CVE-2026-69249 (`cryptography`, fixed 49.0.0, installed 48.0.0 — tracked as W1) and CVE-2026-48710 "BadHost" (`starlette`, fixed 1.0.1, installed 1.3.1 — clean) are the only currently-relevant advisories against the watchlist; nothing new for `pymongo`/`bson`, `shapely`, `s2sphere`, `python-dateutil`, `fastapi`, `uvicorn`, `jinja2`, `python-multipart`, `httpx`/`httpx2`, `anyio`. |
| Dependency CVE sweep (Rust) | No crate entered or left any `crates/*/Cargo.toml`/`Cargo.lock` this window (`decimal.rs` is a new *module* inside the existing `secantus-core` crate, pulling no new crate — `crates/secantus-core/src/lib.rs`'s only change is the one new `mod decimal;` line). `cargo-audit` CI gate stands as the backstop. |
| Extras hygiene | Re-verified: base `[project.dependencies]` still carries only `pymongo`/`s2sphere`/`shapely`/`python-dateutil` (+`tomli` on <3.11); `admin`/`opsboard` still isolate FastAPI/starlette/uvicorn; `cryptography` stays dev-only. `pyproject.toml` untouched this window (not in the diffstat). |
| Secrets scan | Full-diff sweep (`git diff 4efdc64b..HEAD`, excluding `uv.lock`/binary) for API-key/token/password/secret/PEM/JWT/embedded-credential-URI patterns: zero hits. `git log --diff-filter=A` sweep for `.env`/`.key`/`.pem`/`secret`/`credential`-named new files this window: none (the only new files are `changelog.d/*.md`, `crates/secantus-core/src/decimal.rs`, `crates/secantus-core/src/{... test support}`, `tests/test_getmore_maxtimems_zero.py`, `tests/test_rust_decimal_parity.py`). |
| Wheel/sdist build config | Unchanged this window — `pyproject.toml`, `CMakeLists.txt`, and the `cmake/patch_wt_*.py` patchers are untouched (none appear in the diffstat). No fresh empirical `unzip -l dist/*.whl` build this cycle (last done 2026-08-15, per that day's report); a new pure-Rust module with no new crate dependency cannot change wheel *contents* in a way worth a rebuild this cycle. |
| PyPI publish path | Live check this cycle: PyPI's current latest is `0.6.0b15` with **zero yanked releases**, matching `src/secantus/__init__.py` exactly — no out-of-band publish. `publish.yml` unchanged (not in this window's diff). |
| WT submodule pin | `vendor/wiredtiger` gitlink still `eaa187c86147b561c940e9b0e5e628f5e86d075d` (mongodb-7.0.33) — unchanged, re-confirmed via `git ls-tree HEAD -- vendor/wiredtiger` (the tracked gitlink, not the possibly-uninitialized submodule checkout — the earlier `git -C vendor/wiredtiger rev-parse HEAD` in this session's own scratch work returned the *outer* repo's HEAD by coincidental shell fallback and was discarded in favor of this direct tree read). |
| GitHub Actions — full inventory | Same 13 workflow files as yesterday (`ls .github/workflows/*.yml` name list matches exactly) — no new workflow this window, and `git diff --quiet 4efdc64b..HEAD -- .github/workflows/` confirmed empty. `security-review.yml` itself unchanged: `permissions:` still scoped to `contents: write`/`pull-requests: write`/`issues: write`/`actions: read`, no `id-token: write`. |
| Mongo wire-protocol parsing (`wire.py`) | Unchanged since 2026-08-19's full read; not in this window's diff. |
| Rust wire protocol (`crates/secantus-wire`) | `src/` unchanged (`#995` re-confirmed above); not in this window's diff. |
| Rust unsafe/panic audit (`crates/**`) | Per-crate `unsafe` counts unchanged from prior sweeps outside the reviewed new module (`secantus-wt`: 68, `secantus-storage`: 3, `secantusdb`: 1, everything else: 0 including the new `decimal.rs`, confirmed above). No new `panic!`/`unwrap()`/`expect()` on wire-derived data anywhere in the five-commit diff (`update.rs`'s new non-test code uses `.ok_or(Fallback)?` and `.rsplit('.').next().unwrap_or(path)` — both fallible paths handled, no bare unwrap). |
| Storage / WiredTiger FFI | `storage.py` unchanged this window; not in this window's diff. The two touched Rust storage files (`storage-adapter`, `storage/lib.rs`) only add error-code routing, reviewed above. |
| Mongo SCRAM-SHA-256/1 | `auth.py` unchanged; not in this window's diff. |
| MONGODB-X509 / mTLS | `server.py` unchanged; not in this window's diff. |
| `--auth` gating (Mongo) | `commands.py` changed only in `_get_more`'s non-blocking-poll logic (reviewed above); `_NO_PRIVILEGE_COMMANDS` and the rest of the dispatch/auth gate untouched. |
| `getMore` cursor ownership | `cursors.py` unchanged; the ownership check this window's `commands.py` diff sits near is untouched (reviewed above, diff is purely about the wait condition). |
| `system.users` / `system.version` | `storage.py` untouched this window. |
| Credential/token logging | No new `log`/`print` call near password/secret/SCRAM handling introduced by this window's five commits. |
| Admin UI / Ops Board | No commit touched the admin-UI source this window. |
| No `eval`/`exec`/`compile`/`__import__`-as-injection | None of this window's five commits touch a query/pipeline evaluation path with a new interpreter surface; the new `decimal.rs`/`numerics.py` code is pure arithmetic on already-decoded BSON values. |
| Cursor cap / TTL | `cursors.py` unchanged; `DEFAULT_MAX_CURSORS = 10_000` + 600s idle-TTL pruning still in place. |
| Change-stream `awaitData` bound / oplog retention | `storage.py`/`changestreams.py` unchanged this window; `commands.py`'s change only shortens the wait in one new case (reviewed above), never removes or lengthens the existing 1s-default bound. |
| Network exposure & defaults | `server.py` unchanged this window — `host="127.0.0.1"` remains the constructor default. |
| `tests/test_getmore_maxtimems_zero.py`, `tests/test_rust_decimal_parity.py`, `tests/test_mongod_differential.py` (extended) | All drive the real server/engine (no mock storage per `CLAUDE.md`); the `mongod` subprocess launch in `test_mongod_differential.py` remains list-form `subprocess.Popen([MONGOD, "--port", str(port), "--dbpath", tmp, "--quiet"], ...)` with no `shell=True` and no string interpolation of external input (re-confirmed this cycle, unchanged code path from yesterday's review, now exercised by 170 more lines of decimal-arithmetic differential cases). All three are dev-only, none ship in the wheel/sdist. |

---

## 📊 Summary

- **Date of review:** 2026-08-26.
- **SecantusDB version reviewed:** Python server `0.6.0b15`
  (`src/secantus/__init__.py`); Rust server `0.5.3-beta.162` (all 12
  `crates/*/Cargo.toml`, lockstep-confirmed) — no release cut since
  2026-08-24.
- **Total issues by severity:** 5 CRITICAL (all carried forward:
  **#1037** (`$bits*`), **#995, #996, #909, #947, #948** — zero new) · 7
  WARNING (**W1, W2, #976, #977, #978, #949, #910** — all carried forward, all
  re-confirmed present and unfixed, zero new) · 6 INFO (I2–I5 re-derived
  unchanged, I8 new — a positive note on the decimal-arithmetic working-buffer
  bound) · large CLEAN inventory, including every genuinely new or changed
  production-code surface in this window's five commits.
- **Comparison with previous report (2026-08-25):** A similarly quiet window —
  five commits, no wire/auth/dispatch/SQL-surface files touched, and the file
  behind yesterday's tracked CRITICAL (`query.py`) had zero commits this
  window either. Every carried-forward CRITICAL and WARNING was re-verified by
  direct file read against current `HEAD` (not trusted from the prior report)
  and all twelve are unchanged in substance and line location. The two
  genuinely new production-code surfaces this window — the exact-Decimal128
  arithmetic rewrite (both engines, including a brand-new 708-line Rust
  module) and the `getMore` non-blocking-poll fix — were both read in full for
  the first time this cycle and found sound; both are correctness/protocol-
  fidelity hardening, not regressions, and the new Rust module in particular
  handles its working-buffer bound the *right* way (explicit guard + defer)
  where the standing `$bits*` CRITICAL does not. A live `gh issue list`
  cross-check confirmed all twelve tracked CRITICAL/WARNING issues remain open
  with unchanged titles, so no re-filing was needed this cycle.
- **Top 3 priorities for the team (unchanged from yesterday — nothing moved):**
  1. **#1037 — bound the `$bits*` bit-position argument in the Python query
     engine.** `src/secantus/query.py:855-908`'s `_resolve_bitmask` has no
     upper bound on an attacker-supplied bit position, so a single ordinary
     `find` filter can force the interpreter to allocate and physically touch
     several gigabytes of real memory while holding the GIL — no
     authentication or elevated privilege required. The Rust engine already
     does this correctly (bounds every position to `0..64`), and this window's
     new `decimal.rs` module shows the team already knows how to write this
     kind of guard (explicit bound + defer, see **I8**); the fix is to apply
     the same discipline here. Now two full days old with zero movement.
  2. **#995 — fix the Rust `body_len()` bounds check.** Unauthenticated,
     single-frame, full-process DoS on the Rust server; the fix shape is
     already known and nothing has landed on it in eight full review cycles.
  3. **Build the systemic fix for the SQL-authz dispatch-gap bug class**
     (#909, #947, #948, #996 all share one root cause: statement kinds
     parsed as `exp.Command` or executed via an embedded/synthetic path that
     never reaches `_dispatch`/`authz.authorize`). Every report since
     2026-08-16 has flagged this; it is now eleven days old with zero
     movement.
