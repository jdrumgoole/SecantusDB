# Security Review — 2026-08-13

**Reviewer:** Automated nightly security review
**Scope:** Full repository — Python MongoDB-wire server (`src/secantus/**`), the SQL/Postgres-wire server (`src/secantus/sql/**`), admin UI (`src/secantus/admin/**`), Ops Board (`src/secantus/opsboard/**`), Rust server (`crates/**`), GitHub Actions workflows, PyPI wheel hygiene, dependency CVE check.
**Versions reviewed:** Python server `0.6.0b9` · Rust server `0.5.3-beta.159`
**Previous report:** [`docs/security-reports/2026-08-12.md`](2026-08-12.md)

---

## What's new since the last review

Exactly **one commit** landed since the 2026-08-12 report's cutoff
(`6c37e33b`): `a056615e`, the merge of that report's own docs-only PR (#834).
`git diff --stat 6c37e33b..HEAD -- . ':!docs/security-reports'` is empty —
**zero lines of reviewable code, config, dependency manifest, or workflow
changed this window.** This is the quietest cycle on record; every prior
window (including 2026-08-12's "zero commits" sections) still had at least
some file activity to account for.

With no diff to review, this cycle is a **full re-confirmation sweep**:
every standing finding (C1, C2, C3, W1–W7, all INFO items) was independently
re-checked against the current file/line content (not assumed from the prior
report), and the dependency/secrets/workflow/PyPI checks were re-run fresh
against today's date rather than skipped. All eleven previously-standing
findings (C1–C3, W1–W7) are **unchanged in substance and at identical file
and line numbers** — the first cycle in this report's history where line
numbers didn't drift at all, because literally nothing upstream of
`docs/security-reports/` moved. No new findings surfaced.

A fresh CVE/advisory sweep (§1) found nothing new for any tracked
dependency: `pymongo` 4.17.0, `cryptography` 48.0.0 (still one minor behind
`49.0.0`'s CVE-2026-69248/69249, confirmed dev-only/unreachable via
`trustme` — re-verified this cycle, see I42), `pyo3` 0.22.6 (RUSTSEC-2026-0176
still open, tracked via #584), `rustls` 0.23.40 and the rest of the Rust TLS
stack, and WiredTiger (mongodb-7.0.33 pin) all re-swept with no new
disclosure since 2026-08-12.

---

## 1. Dependency Vulnerability Check

No commits touched `pyproject.toml`, `uv.lock`, or any `crates/*/Cargo.toml`
/ `Cargo.lock` — confirmed empty diff. Fresh WebSearch sweep run today
against each tracked package rather than relying on the prior cycle's
results:

- **Python:** `pymongo`/`bson` 4.17.0 — no new advisory (checked against
  general "pymongo CVE 2026" sweep; only historical 2024 issues, both
  already fixed well below the current floor). `cryptography` 48.0.0 —
  checked CVE-2026-39892 (buffer overflow, fixed 46.0.7), CVE-2026-34073
  (DNS name-constraint bypass, fixed 46.0.6), CVE-2026-26007 (ECC subgroup
  attack, fixed pre-48.0.0) — all already below the current pin. 49.0.0's
  CVE-2026-69248 (wildcard-DNS `permittedSubtrees` escape) and CVE-2026-69249
  (exponential path-building DoS via duplicate self-signed intermediates)
  re-confirmed as the only two advisories the current 48.0.0 pin doesn't yet
  carry — unchanged assessment: dev-only via `trustme`-generated test certs,
  not reachable from a deployed server (**I42**, unchanged). `shapely`
  2.1.2, `s2sphere` 0.2.5, `python-dateutil` 2.9.0.post0, `fastapi` 0.136.1,
  `starlette` 1.3.1 (checked against CVE-2026-48710 "BadHost" — affects
  ≤1.0.0 only, current pin patched), `uvicorn` 0.46.0, `jinja2` 3.1.6,
  `httpx` 0.28.1, `anyio` 4.13.0, `python-multipart` 0.0.32, `pywebview`
  6.2.1, `setproctitle` 1.3.7 — no new advisories for any.
- **Rust:** `pyo3` 0.22.6 — RUSTSEC-2026-0176 (out-of-bounds read in
  `PyList`/`PyTuple` iterator `nth`/`nth_back`, affects 0.24–0.28.x — the
  current 0.22.6 pin actually predates the affected range, so this specific
  advisory doesn't apply to the pinned version either way; the standing
  concern per issue #584 is the older RUSTSEC-2025-0020/RUSTSEC-2026-0177
  class, unchanged) still open, tracked via #584, no new advisory this
  cycle. `rustls` 0.23.40 / `rustls-webpki` 0.103.13 (checked, no
  August-2026 advisory found for either) / `rustls-pemfile` 2.2.0 / `ring`
  0.17.14 / `x509-parser` 0.16.0 / `regex` 1.12.3 / `fancy-regex` 0.14.0 /
  `chrono` 0.4.45 / `chrono-tz` 0.10.4 / `bson` 2.15.0 — no new advisory for
  any.
- **WiredTiger** (vendored submodule, mongodb-7.0.33 pin): re-swept —
  general "WiredTiger MongoDB CVE 2026" search surfaces CVE-2025-14847
  ("MongoBleed", a zlib-compression memory-leak issue from December 2025,
  unrelated to WiredTiger and not applicable — SecantusDB doesn't implement
  wire-protocol zlib compression, `OP_COMPRESSED` is rejected outright per
  §3) and MongoDB 8.2's CVE-2025-13643/13644/CVE-2026-1847 release-note
  fixes, none of which name WiredTiger specifically or apply to the vendored
  7.0.33 storage-engine code path in isolation. No WiredTiger-specific CVE
  found.
- **Extras hygiene:** re-confirmed — base `pip install SecantusDB` pulls
  only `pymongo`, `s2sphere`, `shapely`, `python-dateutil`, `tomli`
  (py<3.11). FastAPI/uvicorn/starlette/jinja2/httpx/anyio/python-multipart
  stay under `admin`/`opsboard` extras; `dev` extra stays dev-only —
  re-read directly from `pyproject.toml`'s `[project.optional-dependencies]`
  this cycle, unchanged.

## 2. Hardcoded Secrets Scan

Full sweep re-run (key/token/connection-string/PEM/JWT patterns across
`src/`, `crates/`, `tests/`, `website/`, `docs/`). Zero hits beyond the same
documented illustrative examples as every prior cycle (`rbac.py`'s
`A_CHANGE_PASSWORD` action-name constant and `opsboard/config.py`'s
`ENV_TOKEN` env-var-name constant both matched the grep pattern on the
literal word "secret"/"password" but hold no secret material). `git ls-files
| grep -iE '\.env|\.key|\.pem|\.p12|\.pfx|secret|credential'` — empty. `.gitignore`
confirmed to still cover `.env`, `*.pem`, `*.key`, `.coverage`, `htmlcov/`,
per-crate `target/`, `.venv`. No new hits this cycle.

## 3. Wire-protocol parsing safety

`src/secantus/wire.py` and `src/secantus/sql/pgwire.py`: zero commits since
2026-08-06 (Mongo side) / this window (SQL Fastpath side). Bound discipline
re-confirmed unchanged by direct read: `MAX_MESSAGE_SIZE = 48_000_000` on the
Mongo side, `4 ≤ length ≤ 100_000_000` on the Postgres-wire side,
`OP_COMPRESSED` still rejected as an unsupported opcode rather than driving
an attacker-controlled decompression allocation.

## 4. Storage-engine & FFI safety (WiredTiger)

`src/secantus/storage.py` and every `crates/secantus-storage/**` /
`crates/secantus-wt/**` file: zero commits this window. WT submodule gitlink
re-confirmed unchanged (`git -C vendor/wiredtiger rev-parse HEAD` matches the
gitlink recorded in the last several reports; still mongodb-7.0.33). The
`cmake/patch_wt_*.py` patchers: re-skimmed, still only the documented
idempotent local patches (off64_t→off_t, SUFFIX/dynamic_lookup fixes), no
remote-fetch behavior.

## 5. Authentication & Authorization

`src/secantus/auth.py`, `src/secantus/sql/authz.py`,
`src/secantus/admin/**`: zero commits this window. All standing
authorization-shaped findings independently re-read against current file
content (not assumed):

**C1 (carried forward, still open, issue #783):** re-confirmed present,
unchanged — `src/secantus/commands.py:5965-5966` (`"aggregate": (A_FIND,
SCOPE_COLLECTION)` in `_COMMAND_ACTIONS`) remains the only RBAC gate on
`aggregate`; `_stage_lookup`/`_stage_out`/`_stage_merge`/`_stage_graph_lookup`/
`_stage_union_with` in `aggregate.py`, and `apply_out`/`apply_merge` in
`crates/secantus-commands/src/aggregate.rs`, still call no
`check_privilege`/`authorize`.

**C2 (carried forward, still open, issue #785):** re-confirmed present,
unchanged — `src/secantus/sql/engine.py:1063` (`_collect_dml_sources`),
`:1130` (`_run_delete_using`), `:1158` (`_run_update_from`), `:2452`
(`_run_insert`) still authorize only the statement's primary action; the
`USING`/`FROM`/source tables collected by `_collect_dml_sources` are still
never passed through `authz.authorize()`.

**C3 (carried forward, still open, issue #835):** re-confirmed present,
unchanged, at identical line numbers —
`src/secantus/admin/templates/pages/collections.html:89,94,98,121` still
splices `{{ c.name }}` unescaped into the Alpine.js `@click`/`x-show`
directive string-literal context (`open = (open === 'mod-{{ c.name }}' ? ...`),
while the correct `| tojson` pattern remains used elsewhere in the same
template family (`changestream.html:7-17`). Full description, reproduction,
and recommendation unchanged from the
[2026-08-12 report](2026-08-12.md#c3-new--admin-ui-mongodb-collection-name-is-spliced-unescaped-into-an-alpinejs-directive-enabling-stored-client-side-template-injection-xss-that-pivots-from-the-by-default-unauthenticated-wire-port-to-the-admin-operators-authenticated-browser-session).

**W7 (carried forward, still open, issue #836):** re-confirmed present,
unchanged, at identical line numbers — `src/secantus/sql/pgserver.py:612`
(`_handle_fastpath`) still dispatches straight to `largeobjects.call(...)`
with no `authz.authorize()` call and no read-only-transaction check anywhere
on the Fastpath path; `scalar.py`'s `lo_creat`/`lo_unlink` scalar-function
wrappers still slip past `_write_statement_verb`'s read-only classification
the same way. `LO_PROC_OIDS` still lists only the 13 non-filesystem
functions — `lo_import`/`lo_export` re-confirmed **not implemented**, no
`open`/`os.open`/`pathlib` usage anywhere in `largeobjects.py`.

**W6 (carried forward, still open, issue #806):** re-confirmed present,
unchanged — `configureFailPoint` still absent from `_COMMAND_ACTIONS`
(`commands.py:5960`+) and from Rust's `command_action()`
(`crates/secantus-commands/src/lib.rs`); the `name != "configureFailPoint"`
self-exemption at `commands.py:6796` remains unrelated to RBAC.

`--auth` gating (Mongo-wire, general): re-confirmed unchanged —
`_NO_PRIVILEGE_COMMANDS` allowlist minimal (aside from W6), `getMore`
cursor ownership check present (I18, unchanged).

## 6. Command dispatch & operator-engine safety

Zero commits to `commands.py`, `query.py`, `update.py`, `expressions.py`,
`aggregate.py`, `projection.py`, or the SQL `planner.py`/`engine.py` this
window. Fresh grep re-run this cycle (not assumed from prior cycles): no
`eval`/`exec`/`compile`/`__import__` reachable from a query/pipeline
document or from the SQL parser/callable-statement code
(`planner.py`'s `self._eval` remains an AST-walking method name, unrelated
to Python `eval`); `$where`/`$function`/`$accumulator` still not implemented
as JS-evaluating operators.

## 7. Resource exhaustion / DoS

`cursors.py`/`changestreams.py`: zero commits this window. Re-confirmed
`DEFAULT_MAX_CURSORS = 10_000` (global-only cap, I3 unchanged), the
`$regex` pattern-length-only cap (`_MAX_REGEX_PATTERN_LEN = 1000`, I4
unchanged), and the change-stream `awaitData` wait-seconds computation
(`max_time_ms/1000.0 if max_time_ms > 0 else 1.0`, unchanged — still bounds
a flood of watchers to 1s per poll). Large Object resource limits
(`largeobjects.py`) unchanged: `MAX_LO_SIZE` 4TB, `loread` length bounded to
`i32`, `next_oid()`'s O(n) full-scan-per-create still present (still filed
as INFO, not independently wire-reachable without an established SQL
session).

## 8. Network exposure & defaults

Zero commits touching `server.py`'s bind logic or any admin-UI bind site.
Re-confirmed by direct read: `host` still defaults to `"127.0.0.1"`; every
admin-UI bind site hardcoded to `127.0.0.1`. No `0.0.0.0` default anywhere
in `src/secantus/`.

## 9. The Rust server (`crates/**`)

Zero commits to any `crates/**` file this window. No new `unsafe` block, no
new `.unwrap()`/`.expect()`/`panic!()` on any wire-input-reachable path to
re-review — nothing changed to sweep. Standing findings re-confirmed at
identical line numbers:

**W1 (carried forward, still open — 24th consecutive cycle):** Two
non-poison-tolerant `.lock().unwrap()` sites remain on the Rust oplog mutex.
**File:** `crates/secantus-storage/src/lib.rs:5244` and `:5343` —
re-confirmed byte-identical to the 2026-08-12 report (no churn in this file
this cycle to shift the line numbers, the first time in the series they
haven't moved). Open issue [#593](../../../../issues/593), filed
2026-07-21, unfixed across twenty-four consecutive review cycles.

**W4 (carried forward, open issue, likely fixed in code — 12th consecutive
cycle tracked):** Rust user-transaction async-oplog buffer (`pending_async`)
at `crates/secantus-storage/src/lib.rs:148` re-confirmed unchanged; the
`PENDING_DIRTY_BYTES`/`TransactionTooLargeForCache` bound
(`:2509`/`:4062`/`:5551`/`:5554`/`:5587`/`:5616`/`:5638`) re-confirmed
present and unchanged. Open issue [#750](../../../../issues/750), still
open pending team verification. Not verified empirically in this review
pass (unchanged from prior cycles).

## 10. Admin UI / FastAPI surface

Zero commits to `src/secantus/admin/**`. Auth gating
(`TokenAuthMiddleware` wraps the whole ASGI app), input validation
(Pydantic `Form`/Extended-JSON parsing on every mutation endpoint), and CSRF
posture (unchanged, consistent with the loopback-only threat model) all
re-confirmed unchanged by direct read of `client.py` and the router files.
C3 (§5) is the only live finding in this surface.

## 11. Git history & recent changes

`git log --oneline -30` reviewed — the only commit since the 2026-08-12
cutoff is `a056615e`, the merge of that report's own docs-only PR (#834),
which touches only `docs/security-reports/2026-08-12.md`. `git log --all
--oneline -10 --diff-filter=A -- '*.env' '*.key' '*.pem' '*secret*'
'*credential*'` — empty, as always. No dependency-manifest or workflow
commits to cross-reference against §1's CVE sweep.

## 12. GitHub Actions Workflow Security

`git diff --stat 6c37e33b..HEAD -- .github/workflows/` — empty. Inventory
re-confirmed by direct `ls`: same **13 files** as every prior cycle
(`cargo-audit.yml`, `claude-code-review.yml`, `claude.yml`,
`crash-repro.yml`, `docs.yml`, `publish.yml`, `record-durations.yml`,
`release-binaries.yml`, `rust-wheels.yml`, `security-review.yml`,
`test.yml`, `validate.yml`, `wheels.yml`). Re-verified `validate.yml`
directly this cycle: `plan` (`:43`), `validate` (`:70`), and
`pgjdbc-report` (`:388`) job blocks still carry no `permissions:` key; only
`aggregate` (`:456`) declares one — **W5**, carried forward, still open,
issue #755. `crash-repro.yml` re-confirmed still the only file with
moving-tag `uses:` (**I27**, unchanged).

## 13. PyPI package / wheel integrity

`pyproject.toml`'s `[tool.scikit-build]` sdist/wheel include/exclude list:
zero commits, re-confirmed unchanged by direct read. `wheel.packages =
["src/secantus"]` still restricts the installed wheel regardless of sdist
contents (**I7**, unchanged clarification). `publish.yml` OIDC
trusted-publishing config unchanged. Local tag inventory checked in place of
a live PyPI query (no outbound network access to `pypi.org` from this
review environment this cycle — `pip index versions secantusdb` returned no
match rather than a version list, so the live-PyPI cross-check from prior
cycles could not be repeated verbatim this run): latest local tags are
`v0.6.0b9` (Python, matches `src/secantus/__init__.py`) and
`secantusdb-v0.5.3-beta.159` (Rust, matches every `crates/*/Cargo.toml`) —
no un-tagged version drift visible from the git side.

---

## 🔴 CRITICAL (Immediate action required)

### C1 (carried forward, still open) — `aggregate`'s RBAC check only covers the primary collection; `$out`/`$merge`/`$lookup`/`$graphLookup`/`$unionWith` write or read arbitrary, unauthorized namespaces

**Files:** `src/secantus/commands.py:5965-5966`, `src/secantus/aggregate.py`
(`PipelineContext`, `_stage_out`, `_stage_merge`, `_stage_lookup`,
`_stage_graph_lookup`, `_stage_union_with`),
`crates/secantus-commands/src/aggregate.rs::apply_out`/`apply_merge`.

Re-confirmed present and unchanged this cycle; full description,
reproduction, and recommendation unchanged from the
[2026-08-08 report](2026-08-08.md#c1-new--aggregates-rbac-check-only-covers-the-primary-collection).
Tracked via open issue [#783](../../../../issues/783). **Not fixed in
this review pass** — review/document/file only, per this run's scope.

### C2 (carried forward, still open) — SQL server's `INSERT ... SELECT` / `UPDATE ... FROM` / `DELETE ... USING` check RBAC only on the primary target table; the source table is never authorized

**Files:** `src/secantus/sql/authz.py:66-98,125-145,206-229`,
`src/secantus/sql/engine.py:1063,1130,1158,2452`.

Re-confirmed present and unchanged this cycle. Full description,
reproduction, and recommendation unchanged from the
[2026-08-09 report](2026-08-09.md#c2-new--sql-servers-insert--select--update--from--delete--using-check-rbac-only-on-the-primary-target-table-the-source-table-is-never-authorized).
Tracked via open issue [#785](../../../../issues/785). **Not fixed in
this review pass** — review/document/file only, per this run's scope.

### C3 (carried forward, still open) — Admin UI: unescaped collection name in an Alpine.js directive enables stored XSS that pivots from the unauthenticated-by-default wire port to the admin operator's authenticated session

**Files:** `src/secantus/admin/templates/pages/collections.html:89,94,98,121`.

Re-confirmed present and unchanged this cycle. Full writeup, reproduction,
and recommendation unchanged from the
[2026-08-12 report](2026-08-12.md#c3-new--admin-ui-unescaped-collection-name-in-an-alpinejs-directive-enables-stored-xss-that-pivots-from-the-unauthenticated-by-default-wire-port-to-the-admin-operators-authenticated-session).
Tracked via open issue [#835](../../../../issues/835). **Not fixed in
this review pass** — review/document/file only, per this run's scope.

---

## 🟠 WARNING (Address within 1 week)

### W7 (carried forward, still open) — SQL Large Object Fastpath sub-protocol bypasses RBAC authorization and the read-only-transaction gate

**Files:** `src/secantus/sql/pgserver.py:612-637`,
`src/secantus/sql/largeobjects.py`, `src/secantus/sql/scalar.py:2860`.

Re-confirmed present and unchanged this cycle. Full writeup, reproduction,
and recommendation unchanged from the
[2026-08-12 report](2026-08-12.md#w7-new--sql-large-object-fastpath-sub-protocol-bypasses-rbac-authorization-and-the-read-only-transaction-gate).
Tracked via open issue [#836](../../../../issues/836). **Not fixed in
this review pass** — review/document/file only, per this run's scope.

### W6 (carried forward, still open) — `configureFailPoint` has no RBAC action mapping on either server; an unauthenticated or zero-privilege client can arm a server-wide DoS failpoint

**Files:** `src/secantus/commands.py:5960` (`_COMMAND_ACTIONS`), `:1019`
(handler), `:5906` (dispatch); mirrored on the Rust server at
`crates/secantus-commands/src/lib.rs` (`command_action()`).

Re-confirmed present and unchanged this cycle. Full writeup, reproduction,
and recommendation unchanged from the
[2026-08-10 report](2026-08-10.md#w6-new--configurefailpoint-has-no-rbac-action-mapping-on-either-server-an-unauthenticated-or-zero-privilege-client-can-arm-a-server-wide-dos-failpoint).
Tracked via open issue [#806](../../../../issues/806), filed 2026-08-10.
**Not fixed in this review pass** — review/document/file only, per this
run's scope.

### W1 (carried forward, still open — 24th consecutive cycle) — Two non-poison-tolerant `.lock().unwrap()` sites remain on the Rust oplog mutex

**File:** `crates/secantus-storage/src/lib.rs:5244` and `:5343` — identical
to the 2026-08-12 report, unchanged by any commit this window.

Open issue [#593](../../../../issues/593), filed 2026-07-21, unfixed
across twenty-four consecutive review cycles.

**Recommendation:** unchanged — convert both sites to
`ctx.oplog.lock().unwrap_or_else(|e| e.into_inner())`.

### W2 (carried forward, still open — 21st consecutive cycle) — `drop_index` / `drop_all_indexes` still lack the collection lock that `create_index` holds

**File:** `src/secantus/storage.py:6735` (`drop_index`) and `:6856`
(`drop_all_indexes`) — identical to the 2026-08-12 report; both methods
still open only `with self._lock:`, never `self._coll_lock(db, coll)`.
`create_index` (`:6487`) explicitly takes both locks (`with
self._coll_lock(db, coll), self._lock:`).

Open issue [#635](../../../../issues/635), filed 2026-07-24.

**Recommendation:** unchanged — `with self._coll_lock(db, coll),
self._lock:` in both methods, plus a regression test mirroring
`test_ddl_and_crud_never_deadlock_and_index_stays_complete`.

### W3 (carried forward, still open — 14th consecutive cycle) — `_emit_oplog`'s bare-autocommit path can permanently freeze the change-stream visible tail on a write error

**File:** `src/secantus/storage.py:2472` (`def _emit_oplog`) — re-read; the
bare-autocommit deregistration region still has no surrounding
`try/finally`.

Open issue [#714](../../../../issues/714), filed 2026-07-31.

**Recommendation:** unchanged — wrap the mint-to-deregister region in
`try/finally`.

### W4 (carried forward, open issue, likely fixed in code — 12th consecutive cycle tracked) — Rust user-transaction async-oplog buffer (`pending_async`) has no size/count cap

**File:** `crates/secantus-storage/src/lib.rs:148` — the `Vec` itself still
has no direct cap; the `TransactionTooLargeForCache` dirty-budget check
that appears to functionally bound it is unchanged this cycle.

Open issue [#750](../../../../issues/750), filed 2026-08-02, **still
open**. **Recommendation:** unchanged — the team should verify with a
targeted large-multi-statement-transaction-with-async-oplog test and close
#750 if confirmed. Not verified empirically in this review pass.

### W5 (carried forward, still open — 11th consecutive cycle) — `validate.yml`'s `plan`, `validate`, and `pgjdbc-report` jobs have no `permissions:` block

**File:** `.github/workflows/validate.yml` — re-confirmed unchanged.

Open issue [#755](../../../../issues/755), filed 2026-08-03.

**Recommendation:** unchanged —
```yaml
  plan:
    permissions:
      contents: read
  validate:
    permissions:
      contents: read
  pgjdbc-report:
    permissions:
      contents: read
```

---

## 🟡 INFO (Best-practice recommendations)

All items below re-confirmed present and unchanged this cycle:

- **SQL Large Object `next_oid()` scan (`largeobjects.py:141-144`)** does a
  full unfiltered `find_matching` scan of all LO metadata on every
  `lo_creat` call (O(n) per create, O(n²) for n sequential creations); no
  cap on total large-object count per session. Not independently
  wire-reachable without an established SQL session.
- **I41** — four `contextlib.suppress(Exception)` sites, unchanged; none on
  a write/commit path.
- **I42** — `cryptography` pinned at `48.0.0`, one minor behind `49.0.0`
  (CVE-2026-69248/69249). Not reachable (dev-only via `trustme`).
  Recommendation unchanged: bump at next routine dependency refresh.
- **W1–W7** — see above; now 24/21/14/12/11 consecutive cycles
  respectively for W1–W5 (W4 likely fixed, pending team verification/
  closure), W6 4th cycle tracked, W7 2nd cycle tracked.
- **I3** — `CursorRegistry` global-only cursor cap (`DEFAULT_MAX_CURSORS
  = 10_000`), no per-connection limit.
- **I4** — `$regex` pattern-length cap only (`_MAX_REGEX_PATTERN_LEN =
  1000`), residual ReDoS risk.
- **I6** — all 13 workflow files scope `permissions:` except
  `validate.yml`'s `plan`/`validate`/`pgjdbc-report` (see W5).
- **I7** — sdist still ships nearly all of `tests/`; the installed wheel
  never does (`wheel.packages = ["src/secantus"]`).
- **I9** — `maxWriteBatchSize: 100_000` advertised but not independently
  enforced on `insert`.
- **I10** — tag-protection ruleset unverifiable from this vantage.
- **I11** — `max_connections` (default 1000) global cap, no per-IP rate
  limit.
- **I13** — unused `current_uri_raw` template context var in
  `admin/routers/server.py`; dead code, no live leak.
- **I17** — `pgserver.py` sends raw Python exception string to the wire
  client in its error-formatting path.
- **I18** — `getMore` cursor "ownership" is a namespace-match, not a
  connection-identity check.
- **I24** — Admin UI routers still lack a centralized Pydantic validation
  layer.
- **I25** — `cargo-audit.yml` installs `cargo-audit` unpinned by version.
- **I26** — Admin change-stream websocket has no cap on concurrent
  `/ws/changes/*` connections per session.
- **I27** — `crash-repro.yml` uses moving-tag actions
  (`actions/checkout@v7`, `astral-sh/setup-uv@v7`,
  `actions/upload-artifact@v7`).
- **I28** — `crash-repro.yml` interpolates `workflow_dispatch` inputs into
  `run:` bash blocks (`workflow_dispatch`-only, write-access gated).
- **I31** — Ops Board's generic `/jobs/start` can resolve confirm-gated
  release-class tasks with a weaker gate than `/release/start`.
- **I32** — opsboard auth token accepted via a `?t=` query parameter.
- **I33** — `pywebview`/`setproctitle`/opsboard-extra deps: no advisories
  found this cycle either.
- **I37** — `SECANTUS_WT_CONFIG_EXTRA` appends a raw, unvalidated WT
  connection-config clause from the process environment.
- **I38** — `oplog_table_cfg()` only takes effect at oplog-table CREATE
  time.
- **I39** — cross-transaction uniqueness check's
  once-a-collection-is-written limitation, also reachable via `ROLLBACK
  TO SAVEPOINT`.
- **I40** — `.github/workflows/` holds 13 files, unchanged since
  2026-07-22.

Tracked separately: issue **#584** (pyo3 0.22 → 0.29, clears
RUSTSEC-2025-0020 / RUSTSEC-2026-0177 / RUSTSEC-2026-0176) remains open.

---

## 🟢 CLEAN (Passed review)

| Area | Finding |
|------|---------|
| Dependency sweep — full re-check (all Python + Rust pins), run fresh against today's date | Zero new CVEs/advisories found for any tracked package; no dependency-manifest commits landed this window either. |
| Secrets scan | Zero hits beyond documented test fixtures / doc examples / constant-name false positives. |
| Wire-protocol parsing (`wire.py`, Fastpath `pgwire.py`) | Unchanged; length-bounded frame reads and error-surfacing (no traceback) re-confirmed by direct read. |
| Storage engine — WiredTiger submodule pin | Gitlink unchanged, mongodb-7.0.33. |
| SQL Large Object API — filesystem access (`lo_import`/`lo_export`) | Re-confirmed **not implemented**; no `open`/`os.open`/`pathlib` usage in `largeobjects.py`. |
| Change-stream tailable path | `awaitData` wait bound unchanged (max 1s per poll). |
| Admin UI — auth gating, input validation, CSRF posture | `TokenAuthMiddleware` covers every router; mutation endpoints validate via Pydantic `Form`/Extended-JSON parsing; CSRF posture unchanged and consistent with the loopback-only threat model (aside from C3, a template-escaping bug, not an auth gap). |
| Auth (Mongo) — SCRAM-SHA-256, X.509/mTLS, `--auth` gating | Zero commits; verifier/cert paths unchanged and sound (aside from C1/C3/W6/W7, all authorization- or template-*scope* bugs, not credential-handling ones). |
| Command dispatch & operator engines — `eval`/injection sweep | No `eval`/`exec`/`compile`/`__import__` reachable; no JS-evaluating operator implemented. |
| Network exposure — bind defaults | `127.0.0.1` default confirmed everywhere; no `0.0.0.0` default. |
| Rust server — `unsafe`/panic sweep | Zero commits to `crates/**` this window — nothing new to introduce a finding; standing state (no wire-input-reachable panic path found in prior cycles) unchanged. |
| GitHub Actions — full inventory + pinning + permissions re-sweep | Same 13 workflow files, zero changes this window; only gap is the pre-existing W5 `permissions:` omission. |
| PyPI wheel — sdist/wheel exclude list, publish path | Unchanged since 2026-08-06; OIDC trusted-publishing config unchanged; local tag inventory shows no un-tagged version drift (live PyPI query unavailable from this review environment this cycle — network-access limitation, not a finding). |
| Resource exhaustion — cursors, change streams, oplog retention | `cursors.py`/`changestreams.py` untouched this cycle. |

---

## 📊 Summary

- **Date of review:** 2026-08-13.
- **SecantusDB version reviewed:** Python server `0.6.0b9`
  (`src/secantus/__init__.py`); Rust server `0.5.3-beta.159` (all
  `crates/*/Cargo.toml`, lockstep-confirmed). Both unchanged from
  2026-08-12 (zero commits landed touching either version's source of
  truth).
- **Total issues by severity:** 3 CRITICAL (C1, C2, C3, all carried forward,
  all already tracked as open issues #783/#785/#835) · 7 WARNING (W1–W7, all
  carried forward, all already tracked as open issues
  #593/#635/#714/#750/#755/#806/#836; W4 likely fixed in code but its issue
  #750 remains open pending team verification) · 26 INFO (zero new items
  this cycle) · 14 CLEAN areas (zero new this cycle — the two 2026-08-12
  additions, chunked-drop-purge and Large-Object-filesystem-access, remain
  clean and folded into this cycle's unchanged-area list rather than
  re-itemized as "new").
- **Comparison with previous report (2026-08-12):** The quietest cycle in
  this report's history — a single docs-only merge commit (the prior
  report's own PR) and otherwise zero diff anywhere in the reviewable
  surface. Every one of the eleven standing findings (C1–C3, W1–W7)
  re-confirmed present, unchanged in substance, **and unchanged at the
  byte/line level** — the first cycle where no finding's line numbers
  drifted from unrelated churn. No new findings surfaced; the fresh
  dependency/secrets/workflow/PyPI sweeps (run against today's date rather
  than skipped) found nothing new either.
- **Top 3 priorities for the team:**
  1. **C3** (issue #835, open 1 day) — still the cheapest fix in this
     report: wrap `c.name` in `| tojson` at the four cited call sites in
     `collections.html`. Until fixed, any client that can reach the
     by-default-unauthenticated Mongo wire port can plant a collection name
     that hijacks the admin operator's authenticated browser session on
     next page view.
  2. **C1 + C2** (issues #783/#785, open 5+ days) — both remain unfixed;
     any `--auth` deployment with a collection- or table-scoped role is
     fully exposed to cross-namespace read/write on the Mongo side and
     cross-table read on the SQL side.
  3. **W1** (issue #593) is now the longest-standing item in this report's
     history at **24 consecutive cycles unfixed** — a two-line change
     (`unwrap_or_else(|e| e.into_inner())`) that has outlived every other
     open finding by a wide margin. **W7** (issue #836, open 1 day) is the
     same bypass *shape* as C1/C2 recurring in brand-new code; worth a
     structural sweep for "secondary code path that skips the shared authz
     call" before a fourth instance appears.
