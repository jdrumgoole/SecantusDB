DROP TABLE IF EXISTS ch10
DROP TABLE IF EXISTS p10
CREATE TABLE p10 (id int PRIMARY KEY, n int NOT NULL DEFAULT 0, s text UNIQUE, c numeric CHECK (c > 0))
CREATE TABLE ch10 (id int PRIMARY KEY, pid int REFERENCES p10(id), v int)
INSERT INTO p10 VALUES (1, 5, 'a', 1.5), (2, 6, 'b', 2.5)
INSERT INTO ch10 VALUES (10, 1, 100), (11, 2, 200)
