"""Driver-conformance gauge for the high-level PHP library (mongodb/mongodb).

Runs the unmodified PHPUnit suite from ``vendor/mongo-php-library/`` against a
standalone SecantusDB daemon. See ``runner.py`` for the invocation contract.
"""
