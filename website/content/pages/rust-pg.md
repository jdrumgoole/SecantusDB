Title: Rust PostgreSQL
Slug: rust-pg
Save_as: rust-pg.html
Template: rust_pg
Status: hidden
Summary: SecantusDB's Rust PostgreSQL server — real SQL over the PostgreSQL wire protocol, on the same WiredTiger store the MongoDB server writes to.

<!-- This page renders entirely from the `rust_pg` template
     (themes/secantus/templates/rust_pg.html). The body is intentionally
     empty; all content lives in the template so it can use full-width
     marketing sections rather than the constrained `.page-body` wrapper. -->
