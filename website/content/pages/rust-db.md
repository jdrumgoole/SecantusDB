Title: Rust DB
Slug: rust-db
Save_as: rust-db.html
Template: rust_server
Status: hidden
Summary: SecantusDB's pure-Rust server — a single standalone WiredTiger-backed binary that speaks the MongoDB wire protocol, with no Python in the request path.

<!-- This page renders entirely from the `rust_server` template
     (themes/secantus/templates/rust_server.html). The body is intentionally
     empty; all content lives in the template so it can use full-width
     marketing sections rather than the constrained `.page-body` wrapper. -->
